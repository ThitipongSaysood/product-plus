# AGENTS.md

AI assistants: read `.agents/AGENTS.md` then `.agents/active.md` and the newest file in `.agents/sessions/` before doing anything. Record progress there (use the `agents-checkpoint` skill when available).
