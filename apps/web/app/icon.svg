<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Same mark as .ox-brand__mark: --omnix-accent square, radius-lg, white "P+". A favicon cannot read the page's
       CSS variables, so the token values are written out here. -->
  <rect width="32" height="32" rx="8" fill="#5b5bd6"/>
  <text x="16" y="21.5" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="15" font-weight="600" fill="#fff">P+</text>
</svg>
