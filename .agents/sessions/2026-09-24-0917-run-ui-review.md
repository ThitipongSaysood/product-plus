# 2026-09-24 09:17 — รันแอปดู UI บนเครื่อง dev

## Goal
ผู้ใช้ขอ "รันแอปดู UI" หลังอ่าน .agents/ ตอนเริ่ม session

## 09:17 — session start
**Did:** อ่าน .agents/AGENTS.md → active.md → sessions/2026-09-24-0200 → topics/apify-actors.md
**Learned:** active.md บอกว่า dev servers ถูกทิ้งไว้รัน แต่จริง ๆ ไม่มีอะไร listen ที่ 4010/3020 — notes เก่ากว่าความจริง

## 09:20 — checkout ไม่มี node_modules
**Did:** `pnpm install --frozen-lockfile` (ต้องปิด sandbox เพราะต้องใช้เน็ต) เสร็จใน 28.5s หลังโหลด pnpm เอง
**Learned:** repo ปักหมุด `packageManager: pnpm@10.34.5` แต่เครื่องมี pnpm 12.3.4 → คำสั่ง pnpm ใด ๆ จะ "ค้าง" เงียบ ๆ 4–5 นาทีระหว่างโหลด 10.34.5 ครั้งแรก ไม่ใช่ hang จริง ต่อไปโหลดแล้วอยู่ใน `~/Library/pnpm/package-manager-store/`
**Next:** —

## 09:31–09:33 — seed ซ้อนกันสองโปรเซส
**Did:** พบ `pnpm seed` + `pnpm dev` ของผู้ใช้ค้างอยู่ (S+ รอ input ไม่มี child) ขออนุญาตแล้ว kill; seed ของผู้ใช้ import สำเร็จที่ 09:31 ก่อนหน้านั้น, seed รอบผมที่ 09:33 จึง skip ทุกแพลตฟอร์มตาม dedup `apifyRunId` (import-dataset.ts:28)
**Why:** PGlite เปิดได้ทีละโปรเซส — สอง seed ในช่วงเวลาไล่เลี่ยกันทำให้ state ไม่จบสวย
**Learned:** `pnpm seed` ไม่ควรเริ่มถ้ายังไม่แน่ใจว่าไม่มี seed/dev อื่นเปิดอยู่
**Next:** —

## 09:35 — แอปรันได้ ไล่ดู UI ครบ
**Did:** api 4010 + web 3020 ตอบปกติ ไล่ดู /overview /products /categories /trends /actors /settings/keywords /products/[id] ด้วย integrated browser ที่ viewport 1440×900
**Learned:** dev ไม่ต้องล็อกอิน — auth เปิดโล่งเมื่อไม่ได้ตั้ง APP_PASSWORD (common/http.ts:43)
**State:** ข้อมูลจริงครบ 135 สินค้า · sold30d 21,887 · ใช้ไป $0.85/$10 · กราฟ Douyin 30 วันขึ้นปกติ · หน้าสินค้า/หมวดหมู่/เทรนด์/รายละเอียด แสดงผลถูกต้อง

## 09:38 — เจอสองเรื่องที่ยังไม่ตรงกับที่ notes อ้าง
**Did:** ตรวจผ่าน API ไม่ได้แก้โค้ด
1. **รูปแคชไม่ครบ** — 6/48 ในหน้าแรกมี `imageId` ที่เหลือ 42 เป็น pending (`imageLost=false`, `imageMediaId=null`, มี `imageSourceUrl` ครบ) ไม่ใช่ lost. หน้าสินค้ายังเห็นรูปเพราะ fallback ไป URL ต้นทาง (1688/Douyin ยังโหลดได้) แต่ feed "ความเคลื่อนไหว" ใช้ `imageId` อย่างเดียว → กรอบเปล่าทั้งแถบ และ Temu/XHS จะพังเมื่อ URL หมดอายุ
   - `cacheMedia` (jobs/media.ts:100) กรอง `imageLost=false AND imageMediaId IS NULL` → 42 ตัวนี้ยัง retry ได้ แค่รัน postProcess ซ้ำ
   - แต่ seed รอบ 09:33 รายงาน `media {ok:0,failed:0}` = todo ว่าง ซึ่งขัดกับ 42 ตัวที่เห็นตอนนี้ → ยังอธิบายไม่ได้ สงสัย state เพี้ยนจาก seed ซ้อน ควรตรวจซ้ำหลัง seed สะอาด ๆ รอบเดียว
2. **หน้า /actors ว่าง** — `GET /api/actors` คืน `{"evaluations":[],"evaluatedAt":null}` เพราะ `pnpm --filter @pp/api evaluate` ยังไม่เคยรันบน DB นี้ (seed ไม่ได้เรียกให้) ไม่ใช่บั๊ก
   - แต่ UI แสดงสองข้อความขัดกัน: แบนเนอร์แดง "ไม่มี actor ที่ใช้งานได้ — ทุกตัวถูกคัดออกหรือถูกแพลตฟอร์มบล็อก" ทั้ง 4 แพลตฟอร์ม พร้อมกับการ์ด "ยังไม่มีผลประเมิน กดประเมินใหม่" → ควรซ่อนแบนเนอร์แดงเมื่อ `evaluations` ว่าง
**Next:** ขอ user ตัดสินใจว่าจะหยุด dev เพื่อรัน seed + evaluate รอบสะอาดไหม (ทั้งคู่ฟรี ไม่เรียก actor)

## 10:05 — เพิ่มสร้าง/เปลี่ยนชื่อ/ลบกลุ่มสินค้า

**Did:**
- API [groups.controller.ts](apps/api/src/modules/groups.controller.ts): `POST /groups` (zod `groupCreate`, งบดีฟอลต์ 0, taxonomy ว่างหรือก๊อปจากกลุ่มอื่น), `name` เข้า `groupPatch`, `DELETE /groups/:slug` (ต้อง `confirm:true` · ห้ามลบกลุ่มสุดท้าย · ห้ามลบขณะมี run วิ่ง · เก็บกวาด media กำพร้า), `GET /groups` คืน `productCount`
- `slugify()` ใน [guards.ts](apps/api/src/domain/guards.ts) + เทสต์ใน [guards.test.ts](apps/api/test/guards.test.ts)
- Contracts: `GroupCreate` / `GroupPatch` / `Group.productCount?`
- Web: หน้าใหม่ `/settings/groups` + [GroupsSettings.tsx](apps/web/components/settings/GroupsSettings.tsx) · เมนูใน nav.ts · ตัวเลือก "+ สร้างกลุ่มใหม่" ใน dropdown ของ Sidebar · คีย์ dict ใหม่ครบ th/en/zh

**Why:**
- งบเริ่มต้น $0 — กดสร้างกลุ่มแล้วต้องไม่มีทางเผลอเริ่มใช้เงิน ผู้ใช้ต้องตั้งงบเองอีกที
- ไม่ใช้ `DEFAULT_TAXONOMY` เป็นค่าตั้งต้น เพราะเป็นหมวดของสายนาฬิกาโดยเฉพาะ ใช้กับเคสมือถือไม่ได้
- slug เปลี่ยนไม่ได้หลังสร้าง ลิงก์ที่แชร์ไว้จะได้ไม่พัง — เปลี่ยนได้แค่ชื่อที่แสดง

**Learned:**
- `DEFAULT_PG` เคย hardcode `"apple-watch-bands"` — พอลบกลุ่มนั้นได้แล้วมันจะชี้ไปกลุ่มที่ไม่มีอยู่ เปลี่ยนเป็น `""` แทน แล้วปล่อยให้ API เลือกกลุ่มแรกเอง (`groupBySlug(undefined)`) · จุดที่ต้องตามแก้คือที่ใช้ `pg` เป็น path segment คือ `settings/keywords/page.tsx` ต้อง resolve slug ก่อนยิง
- FK ทุกตัว cascade จาก `product_groups` อยู่แล้ว แต่ `media.id` ถูกอ้างแบบ `set null` → ลบกลุ่มแล้วรูปค้างเป็นขยะ ต้องเก็บกวาดเอง
- `.ap-form-row` ใช้ `align-items: flex-end` → ข้อความช่วยเหลือยาวไม่เท่ากันทำให้ช่องกรอกสูงต่ำไม่ตรง เพิ่ม `.ap-form-row--level` ที่ใช้ grid + subgrid (เฉพาะ ≥768px) ให้ label/ช่อง/คำอธิบายอยู่แถวเดียวกันทุกคอลัมน์
- ตาราง 6 คอลัมน์ในการ์ดทำให้ทั้งหน้าเลื่อนแนวนอนที่ 390px (bottom nav กว้าง 511) แม้จะอยู่ใน `TableScroll` — ซ่อนคอลัมน์รองด้วย `.ap-col-wide` ในบล็อก max-width:767 แก้ได้ · `/overview` ที่มีตารางกว้างกว่าไม่เจอปัญหานี้ ยังไม่ได้หาสาเหตุที่ต่างกัน
- `web-design-guidelines`: เพิ่ม `name` + `autoComplete="off"` ทุกช่อง และ `overscroll-behavior: contain` ให้ `.ox-modal` · ที่ยังไม่ทำตาม: ปุ่ม submit ถูก disable ตอน validation ไม่ผ่าน (ไกด์บอกให้เปิดไว้แล้วโฟกัส error ตอนกด) — ทำตามแบบเดิมของ `GroupForm` ในโปรเจคไว้ก่อนเพื่อความสม่ำเสมอ

**State:** typecheck ผ่าน · 188 เทสต์ผ่าน (api 83 · web 105) · ทดสอบจริงผ่าน curl ครบ: สร้าง / slug ซ้ำ / ชื่อไทยไม่มี slug / ไม่มี content-type / เปลี่ยนชื่อ / ลบโดยไม่ confirm / ลบสำเร็จ · ทดสอบ UI จริงที่ 1440 และ 390 ไม่มี horizontal scroll · กล่องยืนยันลบบอกจำนวนสินค้าและต้องพิมพ์ slug ก่อนปุ่มลบจะกดได้

**Next:** ผู้ใช้ขอ "ปุ่มแปลภาษาจีน" — ยังไม่ชัดว่าหมายถึงแปลชื่อสินค้าจีนเป็นไทย (ต้องใช้ LLM) หรืออย่างอื่น ต้องถามก่อน

## 10:25 — แก้ช่องกรอกสูงต่ำไม่ตรงกันทุกฟอร์ม

**Did:** ผู้ใช้ชี้ว่าฟอร์ม "ตั้งค่ากลุ่ม" ที่หน้าคีย์เวิร์ดก็เป็นเหมือนกัน — ใส่ `.ap-form-row--level` ให้ทั้งสองแถวใน [KeywordsSettings.tsx](apps/web/components/settings/KeywordsSettings.tsx) (บรรทัด 67 และ 159) ด้วย
**Why:** `.ap-form-row` เดิมใช้ `align-items: flex-end` — ความยาวข้อความช่วยเหลือต่างกันทำให้ช่องกรอกไม่อยู่ระดับเดียวกัน และต่างกันอีกในแต่ละภาษา
**Learned:** ต้องให้ `row-gap` ของ grid แม่เท่ากับ gap ในตัว `.ox-field` (ตั้ง `gap: 0` ที่ลูก แล้วให้แม่คุม `var(--omnix-space-1)`) ไม่งั้น track ของ subgrid จะเหลื่อมกับของแม่ · ปุ่ม submit ที่อยู่ท้ายแถว (ไม่ใช่ `.ox-field`) ต้อง `grid-row: 2; align-self: start` ถึงจะอยู่ระดับเดียวกับช่องกรอก
**State:** วัดจาก DOM ที่ 1440px — label ทุกตัวอยู่ที่ตำแหน่งเดียวกัน ช่องกรอกทุกตัวอยู่ที่ตำแหน่งเดียวกัน ทั้งสองแถว · typecheck + 188 เทสต์ผ่าน
**Next:** —

## 10:55 — แปลชื่อสินค้าจีนเป็นไทย

**Did:**
- schema: `products.title_th` + `title_th_at` → migration `drizzle/0003_loud_ultimo.sql` (สร้างด้วย db:generate ไม่ได้เขียนมือ)
- [llm.ts](apps/api/src/jobs/llm.ts): `llmTranslate()` ใช้โครงเดียวกับ `llmCategorize` — batch 25, structured output, ข้าม batch ที่โดน refusal, system prompt บอกชัดว่าชื่อสินค้าเป็น untrusted text
- [translate.ts](apps/api/src/jobs/translate.ts): `runTranslate()` เลือกเฉพาะ `title_th IS NULL`, เขียนผลทีละ batch (ล้มกลางคันแล้วของที่แปลไปแล้วไม่หาย), เพดาน 500 รายการต่อครั้ง
- `POST /jobs/translate` + `RunKind` เพิ่ม `"translate"` → JobButton เดิมแสดง progress ได้เลย
- web: helper `shownTitle()` ใน [bits.tsx](apps/web/components/bits.tsx) — คืนคำแปลไทยถ้ามี ไม่มีก็คืนต้นฉบับ พร้อม `lang` ที่ถูก (เบราว์เซอร์จะได้เลือกฟอนต์ถูก) ใช้ที่การ์ดสินค้า/หมวดหมู่/เทรนด์/รายละเอียด · หน้ารายละเอียดโชว์ต้นฉบับจีนใต้หัวข้อไทย · ปุ่มแปลอยู่หัวหน้า /products

**Why:**
- ผู้ใช้เลือก Haiku 4.5 (≈$0.04 ต่อ 135 ชื่อ) และเลือกกดปุ่มเอง ไม่ใช่แปลอัตโนมัติท้าย pipeline
- เก็บคำแปลใน DB ไม่แปลสดทุกครั้งที่เปิดหน้า — ชื่อสินค้าไม่เปลี่ยน แปลซ้ำคือจ่ายซ้ำฟรี ๆ
- ไม่ทับ `title` เดิม เพราะชื่อจีนคือสิ่งที่เอาไปวางค้นต่อในแพลตฟอร์ม

**Learned:**
- **tsx watch ไม่ restart เมื่อแก้ schema อย่างเดียว** — migration เลยไม่ลง API ตอบ `errors.internal` ที่ `/products` จนกว่าจะ restart โปรเซสจริง ๆ เจอแบบนี้อีกให้ restart ก่อนไล่หาบั๊ก
- typecheck ของ web จับคีย์ dict ที่ขาดได้ครบทุกตัว (DictKey เป็น union จาก dictionary.ts) — เพิ่ม key ใหม่แล้วรัน typecheck คือวิธีหาที่ตกหล่นเร็วสุด

**State:** typecheck ผ่าน · 188 เทสต์ผ่าน · migration ลงบน PGlite แล้ว (`titleTh` โผล่ใน `/api/products`, สินค้า 135 รายการอยู่ครบ) · `POST /jobs/translate` ตอบ `errors.translate.needsKey` ถูกต้องเมื่อไม่มีคีย์ · ปุ่มขึ้นบนหน้า /products แล้ว
**ยังไม่ได้ทดสอบ:** ยังไม่เคยเรียก Anthropic API จริง เพราะยังไม่มี `ANTHROPIC_API_KEY` — คุณภาพคำแปลและ prompt ยังไม่ได้พิสูจน์กับของจริง
**Next:** ใส่ ANTHROPIC_API_KEY ที่หน้า ตั้งค่า › ระบบ แล้วกดปุ่มแปลที่หน้าสินค้า

## 11:25 — backend แปลผ่าน `claude` CLI (สำหรับ VPS)

**Did:**
- [claude-cli.ts](apps/api/src/jobs/claude-cli.ts): spawn `claude -p --output-format json --model … --allowed-tools "" --strict-mcp-config`, prompt ไปทาง stdin, timeout 10 นาที, จำกัด output 4MB, cwd = tmpdir · แยกฟังก์ชันบริสุทธิ์ `parseCliEnvelope` / `extractJson` / `validCliPath` ออกมาให้เทสต์ได้โดยไม่ต้องเสียเงิน
- [translate.ts](apps/api/src/jobs/translate.ts): เลือก backend จาก setting `TRANSLATE_BACKEND` (`sdk` | `cli`) · batch 25 สำหรับ sdk, **120 สำหรับ cli** · เก็บ `total_cost_usd` ที่ CLI รายงานลง `scrape_runs.cost_usd`
- settings: `TRANSLATE_BACKEND` (fallback = `cli` เมื่อไม่มี ANTHROPIC_API_KEY) + `CLAUDE_CLI_PATH` (default `claude`) — โผล่ในหน้า ตั้งค่า › ระบบ เอง
- endpoint เช็คก่อนสร้าง run: backend `cli` ต้องรัน `claude --version` ผ่าน ไม่งั้น `errors.translate.noCli`
- [deploy/README.md](deploy/README.md) §5: วิธีติดตั้ง/ล็อกอินบน VPS + เตือนว่าต้องเป็น OS user เดียวกับที่รัน api (credentials อยู่ใน `~/.claude`) + บอกว่า Railway ใช้ทางนี้ไม่ได้

**Why:**
- ผู้ใช้ติดตั้ง `claude` ไว้บน VPS แล้ว ไม่อยากจัดการ API key แยก
- batch ใหญ่กว่าทาง sdk เพราะค่าใช้จ่ายของ CLI เป็นต่อ "การเรียก" ไม่ใช่ต่อ "ชื่อสินค้า"

**Learned (วัดจริง 2026-09-24 ไม่ใช่ประมาณ):**
- `claude -p` แปล 2 ชื่อ = **$0.0389 / 19.6 วินาที** · cache_creation 12,424 + cache_read 21,448 + thinking 1,986 + output 2,159 token
- แปลว่าทุกการเรียกโหลด system prompt + tool ของ Claude Code เข้าไปก่อน ~33K token → ยิงทีละ 25 ชื่อ (6 ครั้ง) ≈ $0.23 แต่ยิงทีละ 120 เหลือ ~$0.05–0.10
- `result` ในซอง JSON ห่อด้วย ```json fence ต้องแกะก่อน parse
- CLI ไม่มี structured output แบบ SDK → ต้องบอกรูปแบบ JSON ในตัว prompt เอง และ `extractJson` ต้องทนกับ prose รอบ ๆ
- `ant` CLI (ที่ทำให้ SDK อ่าน OAuth profile ได้โดยไม่ต้องมี API key) **ไม่ได้ติดตั้งบนเครื่องนี้** — เป็นทางเลือกที่ถูกกว่าถ้าเซิร์ฟเวอร์ลงได้ บอกผู้ใช้ไปแล้ว

**Security:** spawn ด้วย argv array ไม่ผ่าน shell · ข้อมูลผู้ใช้ไม่เคยเข้า argv (ไปทาง stdin) · `--allowed-tools ""` + `--strict-mcp-config` ตัดสิทธิ์ tool ทั้งหมด · `validCliPath` รับเฉพาะชื่อคำสั่งล้วนหรือ absolute path ที่ไม่มีอักขระพิเศษ — เทสต์ยืนยันแล้วว่าปฏิเสธ `claude; id`, `$(id)`, `/bin/sh -c 'x'`

**State:** typecheck ผ่าน · **195 เทสต์ผ่าน** (api 90 · web 105) · ยืนยันผ่าน curl: binary ไม่มี → `errors.translate.noCli`, path แบบ inject → `errors.translate.noCli` (ปฏิเสธก่อนสร้าง run)
**ยังไม่ได้ทดสอบ:** ยังไม่เคยรันงานแปลเต็มรอบผ่าน backend cli (spawn → parse → เขียน DB) เพราะเสียเงินจริง — ทดสอบแล้วเฉพาะตัว CLI เดี่ยว ๆ กับ 2 ชื่อ ($0.0389)
**Next:** ขออนุญาตผู้ใช้รันเต็มรอบ 135 ชื่อ ≈ $0.05–0.10

## 11:55 — แก้ช้า/แพง/คุณภาพตก: ย้ายกติกาแปลไปเป็น skill แยก + รันขนาน

**สิ่งที่ผู้ใช้เจอ:** กดปุ่มแปลแล้ว "นานอยู่นะ" — รันจริงเสร็จที่ 135/135 แต่ใช้ **5 นาที 27 วินาที และ $0.2124** ขณะที่ผมประเมินไว้ ~1 นาที $0.05–0.10 · และคุณภาพคำแปลแย่กว่าตอนทดสอบ 2 ชื่อมาก (`一珠小蛮腰蝴蝶扣` → "หนึ่งลูก เอว ผีเสื้อ")

**วัดเพิ่ม (ของจริง ไม่ใช่ประมาณ):**
| config | ชื่อ | cost | เวลา | cache_create | thinking/output |
|---|---|---|---|---|---|
| ไม่มี skill, batch 120 × 2 (รันจริง) | 135 | $0.2124 | 5m27s | — | — |
| `--system-prompt` + `--effort low`, batch 25 | 25 | $0.0880 | 59s | 26,741 | 5,363/6,518 |
| **มี skill ผ่าน `--plugin-dir`, batch 10** | 10 | $0.0289 | 19s | 8,497 | 1,351/1,875 |

**Did:**
- สร้าง plugin แยกใน repo: `apps/api/claude-plugin/` (`.claude-plugin/plugin.json` + `skills/translate-listing-titles/SKILL.md`) — กติกาการแปล ตารางศัพท์จีน→ไทย และรูปแบบ JSON อยู่ในไฟล์เดียวนี้
- CLI เรียกด้วย `--plugin-dir <abs>` แล้วสั่ง `/product-plus:translate-listing-titles` — โหลดเฉพาะ skill ของเรา ไม่แตะ skill/plugin/CLAUDE.md อะไรของ host เลย
- **SDK backend อ่าน SKILL.md ไฟล์เดียวกันมาเป็น system prompt** (`skillBody()` ตัด frontmatter ออก) — กติกาอยู่ที่เดียว แก้ที่เดียว ไม่ต้องแตะ TypeScript
- `pooled()` รัน batch ขนานแบบจำกัดจำนวน (CLI_CONCURRENCY = 3) · batch ลดจาก 120 → 40
- แต่ละ batch เขียน DB ทันทีที่เสร็จ ล้มกลางคันแล้วของที่ได้ไม่หาย และรอบถัดไปหยิบเฉพาะที่ยังขาด

**Why:**
- ต้นเหตุที่ช้า/แพงคือ **overhead ต่อการเรียก** ไม่ใช่ต่อชื่อสินค้า (80–90% ของ output token เป็น thinking) · `--effort low` แทบไม่ช่วย · `--system-prompt` ก็ไม่ได้ตัด context ลง
- แต่ batch ใหญ่ทำให้คุณภาพพัง โมเดลเริ่มแปลแบบคำต่อคำ → ทางออกคือ batch กลาง ๆ แล้ว**รันขนาน** ไม่ใช่ batch ใหญ่ขึ้น
- skill แก้คุณภาพได้ตรงจุดเพราะใส่ตารางกับดักคำที่เจอจริงลงไปได้

**Learned:**
- `--plugin-dir <path>` โหลด plugin เฉพาะ session นั้น · โครงสร้างที่ CLI ต้องการคือ `.claude-plugin/plugin.json` + `skills/<name>/SKILL.md` (ดูของจริงจาก plugin figma ที่ติดตั้งอยู่ ไม่ได้เดา)
- `PLUGIN_DIR` resolve จาก `import.meta.url` เป็น `../../claude-plugin` → ใช้ได้ทั้ง `src/jobs` ตอน dev และ `dist/jobs` หลัง build (ยืนยันหลัง build แล้ว) และ tsc ไม่แตะไฟล์ .md เลยไม่ต้อง copy
- **ค่าใช้จ่ายที่ผมใช้ไปวันนี้รวม ≈ $0.36** (ทดสอบ 2 ชื่อ $0.039 + รันจริง 135 ชื่อ $0.212 + วัด 25 ชื่อ $0.088 + วัด skill 10 ชื่อ $0.029)

**State:** typecheck ผ่าน · **199 เทสต์ผ่าน** (api 94 · web 105) · build ผ่านและ plugin path ถูกต้องหลัง build · [deploy/README.md](deploy/README.md) §5 แก้ตัวเลขให้ตรงของจริงแล้ว
**ยังไม่ได้ทำ:** ยังไม่ได้แปลใหม่ด้วย skill — 135 ชื่อในฐานข้อมูลยังเป็นคำแปลรอบแรกที่คุณภาพแย่ ต้องล้าง `title_th` แล้วรันใหม่ ≈ $0.24 / ~2 นาที

## 12:10 — ทำให้ skill เดินทางไปเซิร์ฟเวอร์ได้จริง

**Did:** ตรวจแล้วว่า `apps/api/claude-plugin/` ไม่โดน `.gitignore` (กฎ `dist` ไม่กระทบเพราะ plugin อยู่นอก dist) · ไม่มี .dockerignore/.npmignore/nixpacks ที่จะตัดทิ้ง · `apps/api/package.json` ไม่มี `files` field จึงไม่มีการคัดออก
- เพิ่ม `skillPresent()` + เช็คก่อนสร้าง run ทุกครั้ง → ถ้าไฟล์หาย ตอบ `errors.translate.noSkill` พร้อมบอกว่าต้องคัดลอกโฟลเดอร์ไหน แทนที่จะพังกลางงานด้วย ENOENT ดิบ ๆ
- เทสต์ยืนยันว่า `SKILL_PATH` อยู่ใต้ `apps/api/claude-plugin` และ **ไม่ได้อยู่ใต้ `dist/`** + `plugin.json` ชื่อ `product-plus` ตรงกับ slash command ที่โค้ดเรียก
- [deploy/README.md](deploy/README.md) §5 เตือนชัดว่า deploy แบบอัปโหลดแค่ `apps/api/dist` จะไม่ได้ไฟล์นี้ พร้อมคำสั่งเช็คบนเซิร์ฟเวอร์

**Learned:** `tsc` ไม่คัดลอกไฟล์ `.md` — อะไรที่ต้องอ่านตอน runtime ต้องอยู่นอก `dist/` และต้องมีคนคัดลอกให้ (หรือ build บนเครื่องปลายทาง ซึ่งเป็นวิธีที่ §2 ใช้อยู่แล้ว)

**State:** typecheck ผ่าน · **201 เทสต์ผ่าน** (api 96 · web 105)
**ยังไม่ได้ทำ:** ไฟล์ยัง untracked — ทั้ง session มี 42 path ที่ยังไม่ commit ต้องถามผู้ใช้ก่อนว่าจะ commit ยังไง

## 12:40 — จัด layout หน้ารายละเอียดสินค้า + แกลเลอรีกดในที่

**สิ่งที่ผู้ใช้ชี้:** หน้ารายละเอียด "จัดวางมั่ว" · กดรูปแล้วเปิดแท็บใหม่ · รูปโดน crop

**Did:**
1. **กราฟเดียวแต่กริดสองคอลัมน์** — `.ox-chart-grid` เป็น 2 คอลัมน์เสมอ สินค้าที่ไม่ใช่ Douyin มีกราฟเดียว → ขวามือว่าง 565px · ใส่ `is-wide` ให้การ์ดเดียวเต็มแถว (คลาสมีอยู่แล้วใน CSS)
2. **การ์ดกราฟเปล่าจองที่ 260px** ทั้งที่ไม่มีข้อมูล → `.ox-chart__empty--inline` (position static, min-height 72px) · การ์ดลดจาก **345px → 157px**
3. **ฟีดความเคลื่อนไหวไม่ได้อยู่ในการ์ด** ต่างจากทุกส่วน → ใส่ `<Card>` + empty state
4. **แกลเลอรี**: [ProductGallery.tsx](apps/web/components/ProductGallery.tsx) ใหม่ — `<button>` สลับรูปหลักในที่ ไม่เปิดแท็บใหม่ · `aria-pressed` + ขอบสีเน้นบอกตัวที่เลือก · ปุ่ม 64×64 เกินเกณฑ์ 44px
5. **รูปโดน crop**: `.ap-media-full` สูง 420px แต่ `<img>` ถูกบังคับ `width/height:100%` ในกล่อง `display:grid; place-items:center` → เปอร์เซ็นต์อ้างกลับไปหาแถวที่ auto-size ตามตัวเอง ได้ 450×450 แล้วโดน `overflow:hidden` ตัดหัวท้ายข้างละ 45px · แก้เป็น **flex + `max-width/max-height`** → 750×750 เรนเดอร์ 404×404 พอดีกล่อง ไม่ตัด
6. **thumbnail ซ้ำ** — รูปที่แคช (`/api/media/<id>`) กับ URL ต้นทางเป็นรูปเดียวกันคนละลิงก์ `new Set` จับไม่ได้ → แทนที่ตัวที่ตรงกับ `imageSourceUrl` ด้วยตัวแคช

**แก้ regression ที่ผมทำไว้เอง:** `loadGroup("")` หากลุ่มไม่เจอหลังผมเปลี่ยน `DEFAULT_PG` เป็นค่าว่าง → taxonomy ว่าง → หน้ารายละเอียดโชว์ key ดิบ `material_nylon` แทน "สายไนลอน/ถัก" · แก้ที่ [lib/group.ts](apps/web/lib/group.ts) ให้ fallback เป็นกลุ่มแรกเหมือนที่ API ทำ (กระทบทุกหน้าที่เรียก loadGroup ไม่ใช่แค่หน้านี้)

**Learned:**
- `display:grid` + `place-items:center` ทำให้ `height:100%` / `max-height:100%` ของลูกอ้างกลับไปหา grid area ที่ auto-size ตามลูกเอง → ไม่คุมอะไรเลย **ใช้ flex แทนเมื่อต้องการ max-height เป็นเปอร์เซ็นต์**
- ส่ง `t` (ฟังก์ชัน) จาก server component เข้า client component ไม่ได้ — Next โยน "Functions cannot be passed directly to Client Components" ต้องส่งเป็นข้อความสำเร็จรูป (แบบที่ `SafeImg` ใช้ `lostLabel`)
- Recharts ที่กว้างเกินจอหลัง `browser_emulate` เป็นผลข้างเคียงของการ resize ที่ไม่มี resize event **ไม่ใช่บั๊ก** — โหลดหน้าใหม่ที่ 390px แล้วกว้าง 324px ปกติ

**State:** typecheck ผ่าน · **201 เทสต์ผ่าน** · ยืนยันด้วยการวัด DOM: เดสก์ท็อป 1440 กราฟเต็มแถว การ์ดเปล่า 157px หมวดขึ้น "สายไนลอน/ถัก" · Douyin ยังได้ 2 กราฟข้างกันปกติ · มือถือ 390 ไม่มี horizontal scroll รูปไม่ crop ปุ่ม 64×64

## 13:05 — คอลัมน์รูป/ตารางสูงเท่ากัน (ui-ux-pro-max)

**สิ่งที่ผู้ใช้ชี้:** "อยากให้เท่ากัน" — ใต้แกลเลอรีเหลือที่ว่าง

**วัดก่อนแก้:** คอลัมน์ซ้าย (รูป+thumbnail) 496px · ขวา (ตารางข้อมูล 16 แถว) 746px → **ว่าง 250px**

**Did:**
- `.ap-detail` ที่ ≥1024px เปลี่ยน `align-items: start` → `stretch` · `.ap-gallery-pane { flex: 1 }` และ `.ap-gallery-pane .ap-media-full { flex: 1; height: auto; min-height: 320px; max-height: 720px }`
- max-height กันไว้ไม่ให้กล่องรูปโตเกินเหตุถ้าตารางยาวผิดปกติ — สินค้า Douyin ชน cap ที่ 720px พอดี
- มือถือไม่โดนกระทบ (กฎอยู่ในบล็อก min-width:1024 เท่านั้น) กล่องยังคง 280px
- ลบข้อความซ้ำในแถว "เทรนด์" — `TrendBadge` พิมพ์ "ยังไม่มีประวัติพอ" อยู่แล้ว แต่มี span พิมพ์ประโยคเดิมต่อท้ายอีกรอบ

**ผลวัดหลังแก้:** 1688 → 746/746 เท่ากันเป๊ะ · Douyin → 792/792 · รูปไม่ crop ทั้งสองเคส · มือถือ 390 ไม่มี horizontal scroll

**หมายเหตุออกแบบ:** รูปถูกจำกัดด้วยความกว้างคอลัมน์ (450px) ไม่ใช่ความสูง ดังนั้นพื้นที่ที่ยืดเพิ่มกลายเป็นพื้นสีเทา (`--omnix-surface-2`) รอบรูป — เป็นรูปแบบกรอบรูปสินค้าแบบมาตรฐาน ถ้าไม่ชอบ ทางเลือกคือตัดแถวที่เป็น "—" ออกจากตาราง (มี 3 แถว) เพื่อให้ตารางสั้นลงแทนที่จะยืดรูป

**State:** typecheck ผ่าน · **201 เทสต์ผ่าน**

## 13:30 — แกลเลอรี: เลื่อนซ้าย-ขวา + popup รูปใหญ่ + เลิกยืดกล่องรูป

**สิ่งที่ผู้ใช้ชี้:** "เลื่อนดูรูป ซ้าย ขวา ได้ เปิด popup รูปใหญ่ได้" และ "คอลัมน์ยืดเท่ากันจริง แต่ไม่สวย มีพื้นที่ว่างที่ฝั่งของรูป"

**ถอยของเดิม:** การยืด `.ap-media-full` ให้สูงเท่าตาราง (670px) ทำให้ได้กล่องเทาสูงกับรูปเล็ก ๆ ลอยกลาง เพราะรูปถูกจำกัดด้วย**ความกว้าง**คอลัมน์ (450px) ไม่ใช่ความสูง — ยืดเท่าไรรูปก็ไม่โตตาม
**แทนที่ด้วย:** `aspect-ratio: 1` กล่องเป็นจัตุรัส 450×450 รูปจัตุรัสจึงเต็มกรอบพอดีแทบไม่เหลือเทา + `position: sticky` ที่คอลัมน์ซ้าย รูปเลยเลื่อนตามตอน scroll ดูตาราง ที่ว่างด้านล่างไม่เคยปรากฏบนจอ

**Did:** [ProductGallery.tsx](apps/web/components/ProductGallery.tsx)
- ปุ่มลูกศรซ้าย/ขวาทับบนรูป (44×44 ตามเกณฑ์ touch target) + ตัวนับ `n/total` มุมล่างขวา · วนรอบได้
- คลิกรูป → lightbox: `role="dialog"` + `aria-modal` · โฟกัสไปที่ปุ่มปิดตอนเปิด · **Esc ปิด** · **ลูกศรซ้าย/ขวาบนคีย์บอร์ดเลื่อนรูป** (เฉพาะตอน lightbox เปิด ไม่ไปแย่ง scroll ของหน้า) · คลิกพื้นหลังปิด
- รูปใน lightbox จำกัดที่ `min(1200px, 92vw)` × `88vh` — ใหญ่สุดเท่าที่จอรับได้ ไม่ crop
- ลบข้อความซ้ำแถว "เทรนด์" (TrendBadge พิมพ์อยู่แล้ว)

**State:** typecheck ผ่าน · **201 เทสต์ผ่าน** · เดสก์ท็อป 1440: กล่อง 450×450 รูปเต็มกรอบ sticky ทำงาน ไม่ crop · มือถือ 390: กล่อง 358×358 ลูกศรอยู่ในกรอบรูป 44×44 lightbox พอดีจอ ปุ่มปิด 44×44 ไม่มี horizontal scroll
**หมายเหตุ:** ที่วัด `naturalWidth` ได้ 0 ในเบราว์เซอร์ทดสอบเป็นเพราะ `loading="lazy"` กับ pane จริงที่เตี้ยกว่าค่าที่ emulate — ยืนยันแล้วว่า `/api/media/<id>` คืน JPEG 133KB ปกติ

## 11:42 — รีวิว /ui-ux-pro-max หน้ารายละเอียด: ตัดแถวว่าง · ขยายรูป · ปิดช่องโหว่ lightbox

**Did:**
- `products/[id]/page.tsx` — `facts` เปลี่ยนเป็น `[string, ReactNode | null][]` แล้ว `.filter()` ทิ้งแถวที่ไม่มีค่า
  (`price`/`rank`/`platformCategory`/`shop`/`keyword`/`sizes`/`models`/`deltaSold`/`deltaRank`/`douyinRatio`)
  สินค้าตัวอย่าง 16 → 13 แถว ไม่มี "—" เหลือเลย
- `globals.css:398` — `@media (min-width: 1280px) { .ap-detail { grid-template-columns: minmax(0,5fr) minmax(0,6fr) } }`
- `globals.css` `.ap-lightbox` — `overscroll-behavior: contain`
- `ProductGallery.tsx` — focus trap ใน keydown effect (Tab/Shift+Tab วนใน dialog), effect ที่สองคีย์ด้วย `zoom`
  อย่างเดียวสำหรับ `body.style.overflow = "hidden"` + คืนโฟกัสให้ปุ่ม `opener` ตอนปิด
- `dictionary.ts` — `product.snapshotNeedMore` (th/en/zh) แทน `trend.insufficient_history` ในกราฟว่าง

**Why:**
- ช่องว่างใต้รูป 220px ที่ผู้ใช้บ่นมาสองรอบ **ไม่ใช่เรื่องความสูงของกล่องรูป** แต่เป็นเพราะตารางยาวเกินจริง
  (3 แถวเป็น "—") และคอลัมน์ค่ากว้าง 431px สำหรับค่าอย่าง "4" — แก้ที่เนื้อหา+สัดส่วน ไม่ใช่ยืดกล่อง
- `aria-modal="true"` ซ่อนหน้าหลังจาก screen reader เท่านั้น **ไม่กัน Tab** — ยังแท็บออกไปโดน sidebar หลังฉากได้
- `.ox-modal` มี `overscroll-behavior` อยู่แล้ว แต่ `.ap-lightbox` ใช้ `.ox-modal-mask` ซึ่งไม่มี

**Learned:**
- วัดจริงก่อนเลือกสัดส่วน: ที่ 1440 ลอง 6 แบบ → `5fr/6fr` + ตัดแถวว่าง ได้ Δ14px (รูปโต 456→518)
  `1fr/1fr` กลายเป็น Δ−38 (รูสลับไปอยู่ใต้ตาราง) `3fr/2fr` Δ33 แต่คอลัมน์ค่าเหลือ 291px แคบเกินสำหรับ path จีน
- ที่ 1024 ช่องว่างยังเหลือ 274px และ**แก้ไม่ได้ด้วยสัดส่วน** — content เหลือ 729px เพราะ sidebar กิน 240px
  รูปจัตุรัสกับตารางที่ wrap หนักจะสูงไม่เท่ากันเสมอ sticky เป็นคำตอบเดียวที่เหลือ (ก่อนแก้คือ ~424px)
- `browser_screenshot` ในเครื่องนี้ capture จาก surface จริงของหน้าต่าง (390 CSS px) **ไม่ตาม `browser_emulate`**
  → ตรวจเดสก์ท็อปต้องใช้การวัด DOM ถ่ายภาพยืนยันได้เฉพาะ ~390px
- KeyboardEvent สังเคราะห์ไม่ย้ายโฟกัสเอง แต่ทดสอบ trap ได้ เพราะ handler เรียก `focus()` เองในสาขาขอบ

**Next:** —

## 11:58 — สำรวจฟิลด์ที่ normalizer ทิ้ง (ยืนยันกับฐานข้อมูลจริง 299 แถว)

**Did:** ยังไม่แก้โค้ด — สำรวจอย่างเดียว ก๊อป `.pglite` (134MB) ไป scratchpad แล้วอ่านสำเนา
เพื่อไม่ต้องหยุด dev (PGlite เปิดได้ทีละโปรเซส) ลบสำเนาทิ้งแล้ว

**Why:** ผู้ใช้เลือกให้ดึงฟิลด์ที่ actor ส่งมาแล้วแต่ `normalize1688` ไม่ได้ pick ออกมาแสดง
ต้องรู้ coverage จริงก่อนร่างแผน ไม่งั้นเสนอฟิลด์ที่ว่างเปล่าครึ่งหนึ่ง

**Learned (ตัวเลขจริง ไม่ใช่จาก fixture):**
- **`raw` ครบ 299/299 แถว** → backfill ได้ ไม่ต้องรันแอคเตอร์ ไม่เสียเงิน
- **coverage เป็นของ 1688 เท่านั้น** — 1688 105 แถว: moq 50 · orderCount 50 · videoUrl 37
  douyin 87 / temu 5 / xhs 102 = **0 ทุกฟิลด์** → ห้ามเดาชื่อฟิลด์ของแพลตฟอร์มอื่น (กฎ 0.x)
- **ครึ่งเดียวของ 1688 ที่มี MOQ** (50/105) → แถวต้องซ่อนได้ ซึ่ง filter ที่เพิ่งทำรองรับแล้ว
- **MOQ ส่วนใหญ่คือ 1** — 1:33 · 2:6 · 3:2 · 5:2 · 10:1 · 30:1 · 50:1 · **100:4**
  แปลว่าเคส ¥1.35/MOQ 100 ที่เห็นบนจอเป็น**ข้อยกเว้น** ไม่ใช่เรื่องปกติ ค่าของฟิลด์นี้คือ
  "ยืนยันว่าสั่งชิ้นเดียวได้" บ่อยกว่า "เตือนว่าต้องสั่งเยอะ"
- **บันไดราคามีจริงถึง 3 ชั้น ไม่ใช่ over-build** — 0 ชั้น:75 · 1:13 · 2:2 · **3:15**
  ตัวอย่างจริง `1~499条 ¥6 / 500~2999条 ¥5.5 / ≥3000条 ¥5`
  (เดาผิดตอนแรกจาก fixture ที่มีชั้นเดียวล้วน — ดีที่เช็ค)
- **หน่วยเป็นจีน 5 แบบ** 条 33 · 套 6 · 个 5 · 件 5 · 副 1 → ใช้ตารางแมปเล็ก ๆ พอ ไม่ต้องเรียกตัวแปล
- แถวที่เห็นบนจอมี `saledCountStr: "78万+"` ซึ่งตรงกับ 783,670 ส่วน `soldDisplay: "已售10万+件"` ไม่ตรง
  → `normalize1688` เลือก soldDisplay ก่อน (index.ts:65) จึงดูขัดกันเองบนจอ

**Learned (สภาพแวดล้อม):** ตาราง drizzle อยู่ใน schema `scout` ไม่ใช่ `public` — query ตรง ๆ ต้องใส่ prefix
และสคริปต์ที่ import `@electric-sql/pglite` ต้องวางใน `apps/api/` ถึงจะ resolve เจอ

**Next:** ติดกติกา `.agents/AGENTS.md` — งานนี้แตะ contracts/ingest ต้องผ่าน `grill-with-docs` ก่อน
ซึ่ง **ผู้ใช้ต้องเรียก `/grill-with-docs` เอง** (skill ตั้ง disable-model-invocation) ยังไม่เริ่มแก้โค้ด

## 11:54 — ปุ่มงานยืดตามข้อความสถานะ + `grill-with-docs` ติดตั้งไม่ครบ

**Did:** `globals.css` เพิ่ม `.ap-jobstatus > .ox-btn { align-self: flex-start; }`

**Why:** `.ap-jobstatus` เป็น `flex-direction: column` ซึ่ง `align-items` ดีฟอลต์เป็น `stretch`
→ ลูกทุกตัวยืดเท่าความกว้างของตัวที่กว้างที่สุด พอ job จบแล้วมี `<span role="status">` ยาว
("ประเมิน 44 actor · เลือก: douyin=… 1688=… temu=… xhs=…") ปุ่มเลยโป่งจาก **141px → 1145px**
วัดซ้ำได้ด้วยการ append span ยาวเข้าไปใน wrapper

**Learned:** แก้ที่ `align-items: flex-start` บน wrapper **ไม่ได้** — `.ap-progress` (globals.css:315)
มีแต่ `height: 6px` ไม่มี width มันพึ่ง stretch อยู่ ถ้าปิด stretch ทั้งกล่อง progress bar จะกว้าง 0
จึงต้องให้เฉพาะปุ่มถอนตัวด้วย `align-self` ส่วนปุ่ม `--block` ในแถบข้างมี `width: 100%` ชัดเจน
`align-self` จึงไม่กระทบ (align-self มีผลกับ cross-size เฉพาะตอน size เป็น auto)

**วัดหลังแก้:** head 141→141 (เดิม 1145) · sidebar 215→215 · progress bar ยังยืด 1145/215 สูง 6px
typecheck ผ่าน · 201 เทสต์ผ่าน

**Blocked:** `~/.claude/skills/grill-with-docs/SKILL.md` สั่งให้เรียกสกิลย่อย `grilling` + `domain-modeling`
แต่**ทั้งสองตัวไม่มีในเครื่อง** (ไม่อยู่ใน ~/.claude/skills/, plugins/, synced/) เหลือแค่ agents/openai.yaml
ที่เป็น metadata → กติกา `.agents/AGENTS.md` ที่บังคับ grill ก่อนแตะ contracts/ingest ใช้งานไม่ได้จริง
ทางเลือกในเครื่องที่ใกล้เคียง: `scrutinize`

**Next:** งานดึงฟิลด์ 1688 (MOQ/บันไดราคา/orderCount/video) ยังไม่เริ่ม รอปลดล็อกเรื่อง grill

## 12:15 — เงื่อนไขการซื้อจริงของ 1688 + บาท + ธงแบรนด์ + mock/real

**Scrutinize เจอของใหญ่ที่แผนเดิมมองข้าม:** `normalize1688` ใช้ `price.min` ซึ่งคือ **รุ่นที่ถูกที่สุด
ในบันไดราคา ไม่ใช่ราคาที่ซื้อได้ตอนสั่งขั้นต่ำ** วัดจาก DB: **16 จาก 30 แถวที่มีบันได (53%)
แสดงราคาที่ซื้อไม่ได้** เช่น โชว์ ¥17 แต่สั่ง 1 เส้นจ่ายจริง ¥22 (ต่ำกว่าจริง 29%)

**ตัดสินใจ: ไม่แตะ `products.price`** เพราะ [trend.ts:79](apps/api/src/domain/trend.ts#L79) ตรวจ
`price_drop` ด้วยการเทียบ snapshot ต่อ snapshot — เปลี่ยนความหมายกลางทางแล้วประวัติจะปนสองมาตรฐาน
แก้ที่ **การแสดงผล** แทน: แสดง `entryPrice` เป็นตัวหลัก และบอกว่าราคาต่ำสุดได้ตอนสั่งเท่าไร

**ตัดแผนเดิมทิ้งเกือบครึ่ง:** [ingest.ts:95](apps/api/src/jobs/ingest.ts#L95) เขียน `raw` ลง products
**ทุกรอบ ทั้ง insert และ update** → ไม่ต้องเพิ่มคอลัมน์ ไม่ต้อง migration ไม่ต้อง backfill
ไม่ต้องหยุด dev เลย เขียนเป็นฟังก์ชันบริสุทธิ์อ่าน `raw` แทน

**Did:**
- `domain/normalize/supply.ts` (ใหม่) — `supplyTerms()` + `priceIsBulkOnly()` · 11 เทสต์กับ fixture จริง
- `domain/brand.ts` (ใหม่) — `brandMarks()` · 6 เทสต์
- contracts: `SupplyTerms`/`PriceTier`, `ProductCard.entryPrice`, `ProductDetail.fxThb`, `brandMarks`,
  `Overview.canFetchReal`, `SettingRow.group` += `"money"`
- `queries.ts` — `supplyTerms()` ใน detail + `toCard`, `fxThb()` อ่าน `app_settings` ตรง (ต้องการ `updatedAt`)
- settings: `FX_CNY_THB` / `FX_USD_THB` กลุ่มใหม่ `money` + validate ใน `system.controller.ts`
- หน้า detail: ราคาเป็นราคาที่ซื้อได้ · ขั้นต่ำการสั่ง · **ยอดสั่งขั้นต่ำ** · บันไดราคา · จำนวนออเดอร์ · วิดีโอ
- `Sidebar.tsx:48` — เพิ่มป้าย "ดึงใหม่ไม่ได้" เมื่อ group ตั้งเป็น apify แต่ไม่มี APIFY_TOKEN
- `jobs/translate.ts` + controller — ธง `redo` เพื่อแปลทับของเดิมได้

**Learned:**
- **91% ของชื่อสินค้ามีคำว่า Apple/苹果** เพราะเป็นข้อความบอกความเข้ากันได้ ("适用苹果Watch")
  ธงแบรนด์ที่ไม่แยกสองเรื่องนี้จะติดทั้งแคตาล็อก = ไร้ประโยชน์ → ตัดชื่อผู้ผลิตอุปกรณ์ทิ้งก่อน
  แล้วค่อยหาแบรนด์แฟชั่น/กีฬา ผลคือ **ติด 1 จาก 135** ซึ่งเป็นสัญญาณจริง
- บันไดราคา**ไม่ได้เรียงแพง→ถูกเสมอ** เจอแถว `1~1个 ¥4` แต่ชั้นท้าย ¥10
- `PUT /settings` รับ string อะไรก็ได้ → เรตพิมพ์ผิดจะถูกเก็บแล้วเงียบหาย เพิ่ม validate แล้ว
- หน้า detail พังชั่วคราวเพราะ web render ก่อน api restart เสร็จ (`p.brandMarks` undefined)
  → ใส่ `?.` เพราะ api/web deploy แยกกัน version skew เกิดได้จริง
- `grep` ในเชลล์นี้เงียบผิดปกติกับบางไฟล์ ใช้ `awk` แทนได้ผล

**State:** typecheck ผ่าน · **218 เทสต์ผ่าน** (api 113 · web 105) · ตั้ง `FX_CNY_THB=4.85` ไว้เป็น**ค่าตัวอย่าง**
ผู้ใช้ต้องแก้เป็นเรตจริงเอง

**Next:** แปลชื่อใหม่ 135 รายการกำลังรัน (runId 3e68fbcf) แล้ว commit

## 12:25 — แปลใหม่เสร็จ + เจอ horizontal scroll ที่ 320px

**แปลใหม่ 135 รายการ:** สำเร็จ 135/135 · **$0.3479 · 1 นาที 58 วิ** (รอบแรก $0.2124 / 5 นาที 27 วิ —
แพงขึ้นแต่เร็วกว่ามากเพราะ concurrency 3) **ผมประเมินไว้ $0.24–0.32 จริงเกินไป $0.03**

คุณภาพดีขึ้นชัด วัดได้:
- `一珠小蛮腰蝴蝶扣` → "สายนาฬิกาโลหะข้อเดี่ยว ทรงคอด ตัวล็อกผีเสื้อ" (รอบแรก "หนึ่งลูก เอว ผีเสื้อ")
- **0 จาก 135 ขึ้นต้นด้วย "เหมาะสำหรับ"** ซึ่ง skill ห้ามไว้ (รอบแรกเป็นแบบนั้นแทบทั้งหมด)
- ข้อบกพร่องเหลือ **2 จาก 135 (1.5%)**: 编织 กลายเป็น "สัก" แทน "ถัก" 1 รายการ · เหลืออักษรจีน
  扎染 ในคำแปล 1 รายการ → **แก้ที่ SKILL.md** (เพิ่ม 扎染/蜡绳 ลงตาราง + กฎห้ามเหลืออักษรจีน)
  ไม่ได้รันแปลซ้ำเพื่อ 2 รายการ

**เจอบั๊ก responsive ที่ไม่มีใครเห็นเพราะไม่เคยทดสอบต่ำกว่า 390:**
`.ap-gallery` (thumbnail strip) กว้าง min-content = 5 ชิ้น × 64 + 4 ช่องว่าง × 8 = **352px พอดี**
grid item ดีฟอลต์ `min-width: auto` → 352 กลายเป็นพื้นของทั้งหน้า → ที่ 320px หน้าเลื่อนซ้ายขวา
(scrollWidth 368 > 305) ทั้งที่ strip ตั้ง `overflow-x: auto` ไว้แล้วแต่หดไม่ได้
**แก้:** `.ap-detail > * { min-width: 0 }` + `.ap-gallery { min-width: 0 }`
วัดหลังแก้ที่ 320px: scrollWidth 305 = clientWidth **ไม่มี horizontal scroll** · รูป 273 ·
thumbnail เลื่อนในตัวเองได้ · ปุ่มลูกศร 44×44

**ช่องว่างสองคอลัมน์เปลี่ยนไปเพราะเพิ่ม 4 แถวใหม่:** 1440px — สินค้าที่มีบันไดราคา gap 235px
สินค้าทั่วไป gap 103px (เดิมจูนไว้ 14px ตอนตารางมี 13 แถว) sticky รับไว้ ไม่ไล่จูนต่อเพราะ
ความสูงตารางขึ้นกับข้อมูลของแต่ละสินค้า ทำให้เท่ากันทุกตัวไม่ได้

**State:** typecheck ผ่าน · **218 เทสต์ผ่าน** · commit `5c2029d` บนสาขา `feat/buying-terms-and-detail-page`

## 12:40 — ตั้งเวลาดึงข้อมูลเองได้ต่อกลุ่ม

**Scrutinize เจอ blocker ก่อนลงมือ:** ตัวกันรันซ้ำที่ `weekly.ts` ใช้หน้าต่างกลิ้ง 12 ชม. ซึ่งพอเมื่อ
tick วันละครั้ง แต่**พังทันทีที่ชั่วโมงขยับได้** — ย้ายกลุ่มรายวันจาก 05:00 เป็น 22:00 ตอนบ่าย
ห่างกัน 17 ชม. → ไม่ถูกข้าม → **ยิง actor รอบสอง จ่ายเงินซ้ำในวันเดียว**
ทางกลับกัน 22:00 → 05:00 ห่าง 7 ชม. → ถูกข้าม → หายไปเงียบ ๆ หนึ่งวัน

**Did:**
- `guards.ts` — ลบ `schedulesDue` (ฮาร์ดโค้ดจันทร์) แทนด้วย `bangkok()`, `isScheduleSlot()`,
  **`repeatWindowStart()`** ที่ยึดต้นวันตามเวลาไทย (รายสัปดาห์ยึด 6 วัน) · 12 เทสต์ใหม่
- `weekly.ts` — `@Cron("0 5 * * *")` → **`"0 * * * *"`** + `runScheduled(schedules, { onlyDue })`
  กรองในโค้ดไม่ใช่ SQL (ตารางเล็ก + เลขเวลาไทยอยู่ในฟังก์ชันบริสุทธิ์ที่มีเทสต์)
- `system.controller.ts` — เพิ่ม `GET /api/cron/tick` สำหรับ external timer รายชั่วโมง
  ส่วน `/api/cron/{daily,weekly}` **คงพฤติกรรมเดิม** (force-run) ไม่ให้ของที่มีอยู่พัง
- schema + migration `0004_daffy_nocturne.sql` — `schedule_hour` default 5, `schedule_weekday` default 1
- contracts + zod (0-23, 0-6) + `toGroup` + ฟอร์มกลุ่ม (เลือกวัน/ชั่วโมง ซ่อนตามโหมด) + dictionary 3 ภาษา
- `deploy/README.md` — อธิบายสัญญาใหม่ของ scheduler

**Learned:**
- `reconcile()` ถูกเรียกทุก tick = 24 เท่าของเดิม **ตรวจแล้วปลอดภัย** — มันโพลเฉพาะ run ที่ค้าง
  และการอ่านสถานะ run ของ Apify ไม่เสียเงิน ยิ่งถี่ยิ่งปิดยอดค่าใช้จ่ายเร็วขึ้น
- **ไม่ทำ catch-up ตั้งใจ** — ของเดิมโปรเซสดับตอน 05:00 ก็ไม่รันอยู่แล้ว ไม่ได้แย่ลง
  ส่วน catch-up = ยิง actor เสียเงินในชั่วโมงที่ผู้ใช้ไม่ได้เลือก แย่กว่าข้ามหนึ่งรอบ
- ไม่ให้เลือกนาที เพราะ tick รายชั่วโมงทำตามไม่ได้ UI ไม่ควรสัญญาสิ่งที่ระบบทำไม่ได้
- **migration ไม่ถูก apply เพราะ `getDb()` แคชคอนเนกชัน** (`pending ??= open()`) และ drizzle/ ไม่ถูก watch
  → `touch apps/api/src/main.ts` ให้ tsx restart แล้ว migration ถึงจะรัน (ไม่ต้องหยุด dev)

**Verified:** กลุ่มเดิม hour=5 weekday=1 (พฤติกรรมเดิมคงอยู่) · เปลี่ยนเป็นอาทิตย์ 22:00 แล้วอ่านกลับได้ ·
hour=25 โดนปฏิเสธ 400 · ฟอร์ม: weekly เห็นทั้งวัน+เวลา · daily เห็นเฉพาะเวลา · manual ซ่อนทั้งคู่ ·
มือถือ 390 ทั้งสามช่องสูง 44px ไม่มี horizontal scroll
**คืนค่ากลุ่มกลับเป็นจันทร์ 05:00 แล้ว** (ที่เปลี่ยนเป็นอาทิตย์ 22:00 เป็นแค่การทดสอบ)

**State:** typecheck ผ่าน · **225 เทสต์ผ่าน** (api 120 · web 105)

## 13:18 — บั๊ก ?pg=%EF%BF%BDnew: sentinel เป็น NUL byte จริงในไฟล์ต้นฉบับ

**อาการ:** ผู้ใช้เลือก "+ สร้างกลุ่มใหม่" ในดรอปดาวน์แถบข้าง → ไปที่
`/overview?pg=%EF%BF%BDnew` แล้วขึ้น "ไม่พบกลุ่มสินค้านี้"

**สาเหตุ:** `Sidebar.tsx:22` มี `const NEW_GROUP = "<NUL>new"` โดย NUL เป็น **ไบต์ 0x00 จริง**
ในไฟล์ ไม่ใช่ escape sequence → HTML spec กำหนดให้ parser **แทน U+0000 ด้วย U+FFFD ตอน parse**
→ ค่าที่ได้กลับจาก `<select>` เป็น U+FFFD + "new" ไม่เท่ากับค่าคงที่ที่เอาไปเทียบ
→ หลุดไปเข้าสาขา `withPg(pathname, value)` แทนที่จะไป `/settings/groups`

มองไม่เห็นใน editor ไม่เห็นใน diff ไม่มี lint จับ — typecheck กับ 225 เทสต์ผ่านหมดทั้งที่บั๊กอยู่

**Did:**
- sentinel เปลี่ยนเป็น `"__new-group__"` (ขีดล่างเป็นไปไม่ได้ใน slug ตาม `^[a-z0-9]([a-z0-9-]*[a-z0-9])?$`)
- `onChange` เปลี่ยนเป็น **allow-list**: เฉพาะค่าที่ตรงกับ slug ที่เรา render เองเท่านั้นจึงกลายเป็น `?pg=`
  ค่าอื่นทั้งหมดไป `/settings/groups` — กันค่าที่เบราว์เซอร์เขียนใหม่ในอนาคตได้ด้วย
- เทสต์ใหม่ `apps/web/test/source-hygiene.test.ts` — ห้าม NUL byte และอักขระล่องหน/bidi ในซอร์ส

**Learned:**
- **สแกนทั้ง repo แล้วมีไฟล์เดียวที่มี NUL** — ไม่ใช่ปัญหาเชิงระบบ
- ตอนเขียนไฟล์เทสต์ครั้งแรก escape ถูกเขียนลงไปเป็น**อักขระจริง** ทำให้ regex literal พัง
  ต้องสร้างด้วย `new RegExp` จากสตริงที่ escape แล้ว — ไฟล์ที่ตรวจเรื่องนี้ห้ามมีอักขระพวกนี้ในตัวเอง
- **พิสูจน์ว่า guard ทำงานจริง** ด้วยไฟล์ probe ที่มี NUL แล้วดูเทสต์แดงก่อนลบทิ้ง
  (เทสต์ที่ไม่เคยแดงไม่ได้พิสูจน์อะไร)

**Verified:** ดรอปดาวน์คืนค่า `__new-group__` สะอาด · เลือกแล้วไป `/settings/groups` ไม่มี error
**228 เทสต์ผ่าน** (api 120 · web 108)

## พบระหว่างตรวจ — ยังไม่แก้: เรียงตามราคาไม่ตรงกับตัวเลขที่แสดง

`queries.ts:106` เรียงด้วย `products.price` (รุ่นถูกสุดของบันได) แต่การ์ดแสดง `entryPrice ?? price`
(ราคาที่ซื้อได้จริง) → **สินค้า 17 ตัวที่มีบันไดราคาจะเรียงไม่ตรงกับเลขที่เห็น**
ผมทำให้เกิดเองตอนแก้เรื่องราคา แก้ถูกต้องต้อง denormalize `entry_price` เป็นคอลัมน์
(เรียงใน SQL จากค่าที่คำนวณใน JS จาก jsonb ที่ไม่การันตีลำดับไม่ได้) = schema + migration + ingest + backfill

## 13:35 — เปิดชั้น AI ของการจัดหมวดให้ใช้ claude CLI ได้ (ไม่ต้องมี API key)

**บริบท:** ผู้ใช้ถามว่าหน้า "หมวดแพลตฟอร์มที่ยังไม่จับคู่ 5 รายการ" ใช้ AI ไหม — **ไม่ใช้**
ชั้น 1 (category_map) จับคู่ด้วยมือ · ชั้น 2 (กฎคีย์เวิร์ด) · ชั้น 3 (LLM) เห็นเฉพาะที่ 1-2 ตัดสินไม่ได้
และชั้น 3 **ปิดอยู่** เพราะ `llmCategorize(apiKey, ...)` รับแต่ API key ที่ยังไม่ได้ตั้ง

**Did:**
- `claude-cli.ts` — เปลี่ยนจาก skill เดียวตายตัวเป็น `SKILLS` map + `skillRef/skillPath/skillPresent/skillBody`
  รับชื่อ skill (แก้ call site ใน translate.ts, llm.ts, jobs.controller.ts, claude-cli.test.ts)
- **skill ใหม่** `apps/api/claude-plugin/skills/categorize-listings/SKILL.md` — ตารางคำวัสดุจีน
  (钛合金/米兰/硅胶/不锈钢/尼龙...) + กฎ "จัดตามวัสดุ ไม่ใช่สี/ลาย/แบรนด์" + **กฎสำคัญ: เจอวัสดุที่ taxonomy
  ไม่มี key ให้ตอบ unclassified ห้ามยัดเข้าหมวดใกล้เคียง**
- `llm.ts` — `llmCategorize` รับ backend union (sdk|cli) คืน `{got, costUsd}` · sdk ใช้ skill body
  เป็น system prompt เหมือนฝั่งแปล → **กฎอยู่ที่เดียวทั้งสอง backend**
- `collect()` กรอง key ที่ไม่มีใน taxonomy ทิ้ง — key ที่โมเดลมั่วต้องไม่ไปถึงฐานข้อมูล
- setting `TRANSLATE_BACKEND` → **`AI_BACKEND`** (คุมทั้งแปลและจัดหมวด) ย้ายฟรีเพราะเป็น fallback
  ไม่มีแถวใน DB · `aiBackend()` อยู่ที่ llm.ts ที่เดียว
- `categorize.ts` — `aiCategorizeBackend()` เช็ค skill + binary ก่อน ถ้าใช้ไม่ได้คืน null แล้วข้ามชั้น 3
  (โฮสต์พังไม่ควรทำให้ทั้ง job แดง) · คืน `costUsd` ให้ run เก็บเหมือน job ที่เสียเงินตัวอื่น

**ผลรันจริง (retag, 135 สินค้า):** `0 by platform map, 120 by rules, 2 by AI, 13 unclassified`
**$0.0397 · 18 วินาที**

**Learned — สำคัญ:**
- **AI ถูกจำกัดด้วย taxonomy** `llm.ts` บังคับ enum เป็น key ที่มีอยู่ + unclassified → เจอวัสดุใหม่ก็ตอบ
  unclassified เหมือนเดิม **เปิด AI ไม่ได้แก้ปัญหาหลัก** ทำนายไว้ก่อนรันว่าจะได้ราว 2 ตัว (钛合金, 米兰) — ตรงเป๊ะ
- 13 ตัวที่เหลือแยกได้เป็นสองกลุ่ม:
  - **6 ตัว = วัสดุที่ taxonomy ไม่มี key**: เรซิ่น/เซรามิก (2) · หยก/หิน (1) · ขนสัตว์ 毛昵 (2) · ลูกไม้ (1)
    → แก้ด้วยการ**เพิ่ม key ไม่ใช่เพิ่ม AI**
  - **7 ตัว = ชื่อไม่บอกวัสดุเลย** (四叶草 โคลเวอร์, 粉色星星, 爱心, 豹纹, 礼帽马 ×2, 轻量透气)
    บอกแค่สี/ลาย → ไม่มีตัวจัดหมวดไหนรู้ได้จากชื่อ ต้องดูรูปหรือหน้าสินค้า
- fill mode ข้ามแถวที่ tag ภายใน 7 วัน (`fillEligible`) → ทดสอบชั้น 3 ต้องใช้ `retag`

**State:** typecheck ผ่าน · **230 เทสต์ผ่าน** (api 122 · web 108)
**ยังไม่แตะ taxonomy ของกลุ่ม** — เป็นข้อมูลผู้ใช้ รอสั่ง

## 13:45 — ตรวจว่า "agent เฉพาะงาน" แยกจริงแค่ไหน (ผู้ใช้ถาม)

**คำตอบ:** เป็น skill เฉพาะงานจริง แต่**การแยกอ่อนกว่าที่คอมเมนต์ในโค้ดเขียนไว้** และคอมเมนต์นั้นผมเขียนผิดเอง

**ทดสอบจริง** — รัน `claude -p --allowed-tools "" --strict-mcp-config --plugin-dir <plugin>` แล้วถามว่า
เห็น skill อะไรบ้าง ($0.037):
```
debug-mantra, figma-usability-review, frontend-design, management-talk, post-mortem,
pr-lens, scrutinize, session-notes, ui-ux-pro-max, using-superpowers, web-design-guidelines,
agents, product-management:brainstorm, product-plus:categorize-listings,
product-plus:translate-listing-titles, session-report:session-report, figma:figma-*...
```
→ **`--plugin-dir` เพิ่มปลั๊กอินของเรา ไม่ได้แทนที่ของที่โฮสต์มีอยู่** คอมเมนต์เดิมเขียนว่า
"our own skill only — nothing the host user has installed" ซึ่ง**ผิด** แก้แล้ว

**ทางแยกที่ลองแล้วไม่ได้:**
- `CLAUDE_CONFIG_DIR=<empty>` → ตัด credential ไปด้วย ได้ "Not logged in · Please run /login"
- `--bare` → แยกได้จริง แต่ help ระบุว่า "Anthropic auth is strictly ANTHROPIC_API_KEY or apiKeyHelper
  (OAuth and keychain are never read)" = บังคับใช้ API key ซึ่งเป็นสิ่งเดียวที่ backend นี้มีไว้เพื่อเลี่ยง
- `--disable-slash-commands` → ปิด skill ทั้งหมดรวมของเราด้วย

**สรุป: แยกเต็มรูปแบบไม่ได้ถ้ายังใช้ OAuth login** สิ่งที่ยังจริงอยู่คือ `--allowed-tools ""` ทำให้
ไม่มีเครื่องมือให้เรียก skill อื่นอัตโนมัติ และ `--strict-mcp-config` ตัด MCP ของโฮสต์

**ผลกระทบจริง:** เป็นเรื่องของเครื่อง dev เท่านั้น — เซิร์ฟเวอร์จะมี skill เท่าที่ติดตั้งไว้ที่นั่น
แต่แปลว่า **dev กับ prod ให้ผลไม่เหมือนกัน** และ skill ส่วนตัวกินโทเคนทุก invocation
เขียนเตือนไว้ใน `deploy/README.md` แล้วว่าอย่าติดตั้ง skill อื่นในบัญชี claude ของเซิร์ฟเวอร์

**Learned:** อย่าเชื่อคอมเมนต์ตัวเอง — ทดสอบสมมติฐานเรื่องขอบเขต/ความปลอดภัยด้วยการรันจริงเสมอ

## 13:55 — agent ตัวที่สาม: เสนอสินค้าที่น่าเอามาทำแบรนด์เอง

**วัดข้อมูลก่อนออกแบบ แล้วข้อมูลเปลี่ยนแบบทั้งหมด:**
- ยอดขายอยู่ใน 4 หน่วยที่เทียบกันไม่ได้ — 1688 unknown (สูงสุด 783,670) · douyin 30d (5,371) ·
  xhs lifetime (1,098) · temu lifetime + lowerBound (100,000) · **26 ตัวไม่มีตัวเลขเลย**
- **เทรนด์ 118/135 = 87% เป็น insufficient_history** และมีเฉพาะ douyin ที่มีจริง (actor ส่ง salesTrend รายวัน)
→ **ranked list เดียวบนข้อมูลนี้คือการโกหกด้วยตัวเลข** เพราะบอกเป็นนัยว่ามีสเกลร่วมซึ่งไม่มี

**สถาปัตยกรรม: โค้ดคำนวณสิ่งที่เทียบได้ AI ตัดสินสิ่งที่ตัวเลขตอบไม่ได้**
- `domain/brand-brief.ts` (บริสุทธิ์ · 12 เทสต์) — จัดกลุ่มตาม (platform, soldPeriod) จัดอันดับ
  **ภายในกลุ่มเท่านั้น** · คัดเลือกหัวกะทิจากทุกกลุ่มไม่ใช่จากเลขดิบรวม (ไม่งั้น 1688 กินหมดเพราะเลขใหญ่กว่า 700 เท่า)
  · ราคากลาง/ความแออัดต่อหมวด · เรียก `brandMarks()` ที่มีอยู่แล้วแทนให้ AI เดาเอง
  · ตัดตัวที่ไม่มีราคา/ยอดขายออกแล้ว**รายงานว่าตัดกี่ตัวเพราะอะไร**
  · ส่ง `trendKnown: false` รายตัว ไม่ใช่ละฟิลด์ทิ้ง — ฟิลด์ที่หายไปโมเดลอ่านว่า "เฉย ๆ"
- **skill ใหม่** `brand-candidates` — ห้ามเทียบข้ามแพลตฟอร์ม · ห้ามแปลง lifetime เป็นรายเดือน ·
  `trendKnown:false` = ไม่มีข้อมูลโมเมนตัม ห้ามเขียนว่า "กำลังมา" · ห้ามประมาณกำไร/ตัดสินซัพพลายเออร์/สรุปกฎหมาย
- `jobs/brand.ts` — `sanitize()` ทิ้ง id ที่ไม่ได้อยู่ใน brief (id ที่โมเดลมั่วต้องไม่กลายเป็นลิงก์)

**ผลรันจริง: $0.0956 · พิจารณา 50 → เสนอ 7 · ไม่แนะนำ 8**
โมเดลเคารพกฎทุกข้อ เขียนเองว่า *"127,127 sold (period unknown)"* และใส่ข้อเสียว่า
*"Sold figure has unknown period (cannot compare to Douyin 30d)"* · *"No trend data—momentum unknown"*
· *"Temu's floor values make precise evaluation impossible"*

**Learned:**
- **scrutinize ของผมเองผิดหนึ่งข้อ** — บอกว่า `scrape_runs.raw` มีอยู่แล้วไม่ต้อง migration
  ความจริง `raw` อยู่บน products/product_snapshots/actor_evaluations **ไม่ใช่ scrape_runs**
  (อ่าน awk ที่เลยขอบตารางไป) → เพิ่มคอลัมน์ `report` jsonb + migration 0005 จริง ๆ
- `GET /brand` ซ้อน `{report:{report,titles}}` เพราะ controller ห่อค่าที่ห่อมาแล้ว
- ชื่อสินค้าขึ้นเป็น UUID เพราะดึงจาก `/products` หน้าแรก (48 จาก 135) → ให้ API resolve ชื่อจาก id ที่รายงานอ้างถึงแทน

**State:** typecheck ผ่าน · **242 เทสต์ผ่าน** (api 134 · web 108) · หน้า /brand เรนเดอร์ถูกทั้ง 1425 และ 375

## 14:00 — หน้า /brand อ่านง่ายขึ้น + รายงานตามภาษาของระบบ

**ผู้ใช้ชี้สองเรื่อง:** (1) หน้าเป็นกำแพงข้อความอังกฤษ ไม่มีรูป (2) อยากได้ไทย หรือตามภาษาระบบ

**ผมฮาร์ดโค้ดเป็นไทยไปก่อน แล้วผู้ใช้แก้ให้ถูกกว่า** — ระบบมี 3 ภาษาพร้อมปุ่มสลับ
รายงานจึงควรตามภาษาที่เลือกอยู่ ไม่ใช่ไทยตายตัว → brief มีฟิลด์ `lang` · skill เขียนตาม `lang`
· `BrandReport.lang` บันทึกไว้ · ถ้าภาษาปัจจุบันต่างจากรายงาน หน้าจะบอกให้กดวิเคราะห์ใหม่

**Did (UI):**
- `BrandResponse.titles` → **`items: Record<id, BrandItem>`** — รูป ชื่อ ลิงก์ ราคา ยอดขาย+ช่วง MOQ หมวด
  **ตัวเลขทั้งหมดมาจากฐานข้อมูล ไม่ได้มาจากข้อความที่โมเดลเขียน**
- การ์ดใหม่: รูป 96px ซ้าย · ชื่อ+ป้ายความเหมาะสม · แถบ chip (ราคา/ยอดขาย/ขั้นต่ำ/หมวด) ·
  สรุปสั้น · ข้อดี-ข้อเสียเป็นลิสต์ไอคอน ✓/✗ สองคอลัมน์ (มือถือซ้อนกัน) · ลิงก์เปิดที่แพลตฟอร์ม
- ใช้ `ProductImage`, `SoldBadge`, `BrandMark`, `categoryLabel` ที่มีอยู่แล้ว ไม่สร้างใหม่
- ย้ายกล่อง "ข้อมูลนี้บอกอะไรไม่ได้บ้าง" ไปท้ายหน้า — ต้องเข้าถึงได้ แต่ไม่ใช่สิ่งที่หน้านี้มีไว้เพื่อ
- ป้ายความมั่นใจเปลี่ยนจาก "สูง/กลาง/ต่ำ" เป็น "น่าสนใจมาก / พอได้ / ข้อมูลไม่พอ" — บอกความหมายไม่ใช่ระดับ

**Did (skill):** เพิ่มกฎความยาวเป็นเพดานแข็ง — `why` ไม่เกิน 2 ประโยคสั้น · ข้อดี/ข้อเสียอย่างละไม่เกิน 3 ข้อ
ข้อละไม่เกิน 10 คำ · picks ไม่เกิน 6 · และกฎสำคัญ: **"หน้าจอแสดงรูป ชื่อ ราคา ยอดขาย ขั้นต่ำ หมวด
อยู่ข้าง ๆ ข้อความคุณแล้ว ห้ามพูดซ้ำ"** พร้อมตัวอย่าง bad/good — เพราะรอบแรกโมเดลเขียนซ้ำตัวเลขที่อยู่บนจอ

**Learned:** rwd-audit จับ grid ใหม่ 3 ตัว (`.ap-pick`, `.ap-judge li`, `.ap-avoid__row`)
ทั้งหมดเป็นแพตเทิร์น "คอลัมน์ตายตัว + minmax(0,1fr)" เหมือน `.ap-lane__row` ที่อนุญาตไว้แล้ว → ลง ALLOW_GRID

**State:** typecheck ผ่าน · 242 เทสต์ผ่าน · กำลังรันรายงานรอบใหม่เป็นภาษาไทย

## 14:06 — เปลี่ยนโมเดลของงาน brand scout เป็น Sonnet 5

**ปัญหาที่เห็นหลังรันเป็นภาษาไทยด้วย Haiku:** สั้นลงและเป็นไทยจริง แต่**ภาษาหยาบ**
"ฟอร์มเมชคลาสสิก" · "ต้องยืนยันจำนวนต่ำ" · "บ้านลูกปัดตัวตนศิลปิน" (สรุป)
และยังพูดซ้ำตัวเลขที่อยู่บน chip เหนือมันเอง ("ราคากลาง 26.8 CNY")

**Did:** แยก `BRAND_MODEL = "claude-sonnet-5"` ออกจาก `LLM_MODEL` (Haiku) —
งานแปลกับจัดหมวดเป็นงานปริมาณมากและเป็นกลไก (ตารางคำศัพท์/รายการ key ทำงานแทนเกือบหมด) Haiku คุ้มกว่า
ส่วน brand brief เป็น **1 call ต่อรอบ** และเป็นงานตัดสินใจ + เขียนให้คนอ่านจริง
เพิ่มกฎใน skill: ห้ามพูดซ้ำตัวเลขใน pros/cons ด้วย (ยกตัวอย่างที่พลาดจริง) + "เขียนอย่างที่คนพูด
ไม่ใช่แปลเรียงคำจากอังกฤษ"

**ผลเทียบกันบนข้อมูลชุดเดียวกัน:**
| | Haiku 4.5 | Sonnet 5 |
|---|---|---|
| ค่าใช้จ่าย/รอบ | $0.09 | **$0.4575** |
| ภาษาไทย | หยาบ มีคำประหลาด | เป็นธรรมชาติ |
| ระดับความเหมาะสม | **ให้ high หมดทุกตัว** | medium 3 / low 3 |

**ข้อสังเกตสำคัญ — ไม่ใช่แค่เรื่องภาษา:** skill เขียนไว้ว่า "confidence เป็น low เมื่ออิงตัวเลขที่ไม่มีช่วงเวลา
หรือไม่มีเทรนด์" **Haiku ไม่ทำตาม ให้ high ทุกตัว · Sonnet ทำตามถูกต้อง** ให้ low กับตัวที่ยอดขาย
ไม่ระบุช่วงเวลา → โมเดลที่อ่อนกว่าไม่ได้แค่เขียนแย่กว่า แต่**ไม่เคารพกฎความซื่อสัตย์ที่เขียนไว้ด้วย**

**Verified:** 1440 — 6 การ์ด รูปขึ้นครบ 6 ป้าย "พอได้/ข้อมูลไม่พอ" ข้อดี 2-3 ข้อเสีย 2-3 ·
390 — การ์ด 309px รูป 96px ข้อดี/ข้อเสียซ้อนกันเป็นคอลัมน์เดียว ปุ่ม 44px ไม่มี horizontal scroll
**242 เทสต์ผ่าน**

## 14:25 — ย้ายจาก PGlite ไป PostgreSQL (omnix_marketing / product_plus)

**ผู้ใช้กำหนดโครงสร้าง:** database `omnix_marketing` · dataset(=schema) `product_plus` · tables
("dataset" เป็นคำของ BigQuery ซึ่งตรงกับ schema ของ Postgres)
**ตัดสินใจ:** dev เครื่องเดียวก่อน ยังไม่ deploy · ย้ายข้อมูลทั้งหมด · รูปเก็บใน Postgres ต่อ

**สำรวจก่อนวางแผน (ตัวเลขจริง):**
- โค้ดรองรับ Postgres อยู่แล้ว — `db/client.ts` สลับตาม `DATABASE_URL` **งานนี้ไม่ใช่การแก้โค้ด**
- PGlite คือ **PostgreSQL 18.3** → ไม่มีช่องว่าง dialect · ไม่ต้องติดตั้ง extension (gen_random_uuid อยู่ใน core)
- ข้อมูลจริง 46MB โดย **42MB คือ media (294 รูป เป็น bytea)** ที่เหลือ ~4.7MB / ~1,600 แถว
- `scout` ปรากฏแค่ใน schema.ts กับ migration SQL เท่านั้น ไม่มี raw SQL ที่อื่น → เปลี่ยนชื่อได้สะอาด

**Did:**
- `schema.ts` — `pgSchema("scout")` → `pgSchema("product_plus")`, const `scout` → `pp` (10 ตาราง)
- ยุบ migration 6 ไฟล์เป็น `0000_init.sql` ใบเดียว (ยังไม่เคย deploy จึงไม่มีประวัติให้รักษา
  และดีกว่าพอก `ALTER SCHEMA` ทับ)
- `docker-compose.yml` — postgres:18-alpine, db `omnix_marketing`, bind `127.0.0.1:5433`
- `apps/api/.env` + `.env.example` · `SETTINGS_SECRET` 64 ตัวอักษร
- `pg: "latest"` → `^8.23.0` · `@anthropic-ai/sdk: "latest"` → `^0.128.0`
- สคริปต์ `pglite-to-postgres.ts` + `pnpm --filter @pp/api migrate-store [--verify]`
- ลบคำเตือน "Stop the api first" 3 ไฟล์ · อัปเดต README + deploy/README

**เจอระหว่างทาง (สำคัญ):**
1. **ไม่มีอะไรโหลด `.env` เลย** — ไม่มี dotenv ไม่มี @nestjs/config ไฟล์ .env จะถูกเมินทั้งใบ
   แก้ด้วย `node --env-file-if-exists=.env` ในทุก script (ไม่ต้องเพิ่ม dep · ไม่มีไฟล์ก็ไม่พัง)
   **ทดสอบแล้วว่า tsx forward flag นี้ได้จริง**
2. **postgres:18 ปฏิเสธการ mount ที่ `/var/lib/postgresql/data`** — 18+ เก็บ data ใน subdirectory
   ตาม major version ต้อง mount ที่ `/var/lib/postgresql` แทน (docker-library/postgres#1259)
3. **PGlite คืน jsonb เป็น JS object** แล้ว node-postgres เข้ารหัสตามชนิดของ JS ไม่ใช่ชนิดคอลัมน์
   → JS Array กลายเป็น Postgres array literal `{"a","b"}` ไม่ใช่ JSON → insert พัง
   แก้ด้วยการอ่าน `udt_name` จาก information_schema ปลายทางแล้วแปลงตามชนิด (jsonb→stringify, bytea→Buffer)

**ผลย้าย: 1,595 แถว ครบทุกตาราง** และ **รูป 294 ใบผ่านการตรวจ sha1 จริง 0 mismatched**
(ตอนแรก fallback ไปเทียบขนาดเพราะไม่มี pgcrypto → ติดตั้งแล้วตรวจซ้ำแบบเข้ม)

**ตรวจแอปจริงบน Postgres:** 9 หน้าเปิดได้ครบ · products 135 · entryPrice 1.35 · รูปเสิร์ฟ 251KB image/jpeg
· รายงาน brand scout (sonnet-5, 6 picks) รอดมาครบ · schedule hour=5 weekday=1 คงเดิม

**ผลพลอยได้ที่ใหญ่กว่าที่คิด:** ข้อจำกัด "PGlite เปิดได้ทีละโปรเซส" หายไป — ตลอดเซสชันนี้ต้อง
`touch main.ts` ให้ migration รัน, ก๊อป .pglite 133MB ไปอ่านสำเนา, และเคยต้องขอ kill dev ก่อนรันสคริปต์

**State:** typecheck ผ่าน · **242 เทสต์ผ่าน** · `.pglite` **ยังไม่ลบ** เก็บไว้เป็นทางถอย

## 14:40 — ตรวจผล /code-review 15 ข้อ ก่อน deploy

**ผลตรวจ: จริง 13 · ไม่ใช่งานผม 1 · โดยออกแบบไว้แบบนั้น 1**
และ **ผม verify ข้อ 13 ผิดเองรอบแรก** — สแกนเฉพาะบรรทัดที่มีคำว่า "0300" เลยไม่เจอ
ต้องใช้ `cat -v` ถึงเห็นว่าเป็นอักขระจริง บทเรียน: สแกนทั้งไฟล์ อย่าเดาว่าอยู่บรรทัดไหน

**แก้แล้ว (เรียงตามความเสี่ยง):**
1. `claude-cli.ts` — เพิ่ม `child.stdin.on("error")` · EPIPE ตอนเขียน prompt ที่ใหญ่กว่า pipe buffer 64KB
   (brand brief 60 candidate ใหญ่กว่าเสมอ) เป็น unhandled event = **api ทั้งตัวตาย** แทนที่จะ fail แค่ run เดียว
2. `README.md` — **เตือนว่าห้ามใช้ PGlite เปิดข้อมูลเก่า** ยืนยันแล้วว่า PGlite บันทึก migration ล่าสุดไว้ที่
   `1790232482350` ส่วน `0000_init` ใหม่คือ `1790234208104` ซึ่งใหม่กว่า → drizzle จะรันทับสร้าง
   `product_plus` เปล่า แล้วแอปขึ้นสินค้า 0 ชิ้นทั้งที่ข้อมูลอยู่ใน `scout` **บรรทัดที่ผมเขียนเองชวนให้ทำพอดี**
3. `system.controller.ts` — `isRate("")` ปฏิเสธการล้างค่า ทำให้**ลบเรตบาททิ้งไม่ได้เลย** (ฟอร์มส่ง "" เสมอ)
4. `ProductCardView.tsx` — `p.entryPrice ?? p.price ?` ใช้ truthiness ทำให้**ราคา 0 แสดงเป็น "—"**
5. `guards.ts` + `GroupsSettings.tsx` — อักขระ U+0300/U+036F **ตัวจริง** ใน regex → เปลี่ยนเป็น escape
   และ **ขยาย `source-hygiene.test.ts` ให้ครอบคลุมช่วงนี้** (เดิมไม่ครอบ จึงผ่านทั้งที่มีปัญหา)
   พิสูจน์ด้วย probe แล้วว่าเทสต์ใหม่จับได้จริง
6. `supply.ts` — `entryPrice` ใช้ `ladder[0]` เสมอ ผิดเมื่อ MOQ สูงกว่า rung แรก
   (MOQ 100 + rung [1-99 ¥13.50, 100+ ¥9] → ยอดสั่งขั้นต่ำเพี้ยนเป็น ¥1350 แทน ¥900)
   **ข้อมูลจริง 0/30 แถวที่เข้าเคสนี้** แต่เป็นบั๊กแฝง แก้แล้ว + 3 เทสต์ใหม่
7. `groups.controller.ts` — การกวาด media ตอนลบกลุ่มเป็น global → แข่งกับ media job ของกลุ่มอื่นที่เพิ่ง
   insert แต่ยังไม่ได้ link (jobs/media.ts insert ก่อน set imageMediaId) แก้ให้จำกัดเฉพาะ id ของกลุ่มที่ลบ
8. `jobs.controller.ts` — endpoint brand โยน `errors.translate.*` ทำให้ผู้ใช้เห็นข้อความเรื่องการแปล
   เพิ่ม `errors.brand.*` ครบ 3 ภาษา (และลบคีย์ซ้ำที่ผมเผลอเพิ่มไว้สองที่)
9. `deploy/README.md` — ยังเขียน `TRANSLATE_BACKEND` ทั้งที่ rename เป็น `AI_BACKEND` แล้ว (2 จุด)
10. `ProductGallery` — `active` ไม่รีเซ็ตเมื่อเปลี่ยนสินค้า → ใส่ `key={p.id}` + clamp index กันไว้สองชั้น
11. ลบโค้ดตาย — `priceIsBulkOnly` (ไม่มีคนเรียก) · `lowestPrice` (ซ้ำกับ `cheapestTier` ฝั่งเว็บ) ·
    import `unitLabel` ที่ไม่ได้ใช้

**ไม่แก้ พร้อมเหตุผล:**
- **#11 หน้าต่าง 6 วันของรายสัปดาห์** — reviewer บอกว่าย้ายวันไปก่อนหน้าแล้วข้ามรอบ **จริง แต่เป็นการแลก
  ที่ตั้งใจ**: ทางเลือกคือยอมให้รันซ้ำภายในสัปดาห์เดียว = จ่ายเงินสองรอบ เทสต์ของผมยืนยันพฤติกรรมนี้ไว้แล้ว
  สิ่งที่ควรทำคือบอกผู้ใช้ใน UI ไม่ใช่เปลี่ยน guard
- **#9 translate cap 500 ไม่มี ORDER BY** — ของเดิมก่อนเซสชันนี้ กลุ่มปัจจุบันมี 135 ยังไม่ชน
- **#10 transaction ซ้อนบน PGlite** — ตกไปแล้วเพราะย้ายมา Postgres ซึ่ง drizzle เช็คเอาต์ client แยกต่อ transaction
- **#15 `text.text`** — **ผู้ใช้ commit เอง** (`942fe49` ThitipongSaysood 14:26) ผม `git reset` ไฟล์นี้ออกทุกครั้ง

**State:** typecheck ผ่าน · **243 เทสต์ผ่าน** (api 135 · web 108) · 9 หน้าเปิดได้ครบบน Postgres ·
ล้าง/ใส่เรตบาทได้แล้ว และยังปฏิเสธค่าที่พิมพ์ผิด

## 14:55 — เขียน deploy/README.md ใหม่แบบละเอียด

**Did:** เขียนใหม่ทั้งใบ 462 บรรทัด 14 หัวข้อ สำหรับคนที่ไม่ได้เขียนโค้ดตัวนี้
ภาพรวมสถาปัตยกรรม · การตัดสินใจ 4 ข้อก่อนเริ่ม · Postgres · api · web · seed ·
ด่านเงิน · งาน AI · ตารางเวลา · checklist หลัง deploy · งานดูแล · กับดัก 9 ข้อ · CI · ตารางตัวแปร 15 ตัว

**ยืนยันจากโค้ดก่อนเขียน ไม่เขียนจากความจำ:**
- `http.ts:48` — `if (!password) return true` → **APP_PASSWORD ไม่ตั้ง = API เปิดโล่งทั้งหมด**
  รวมถึง endpoint ที่กดแล้วเสียเงิน ใส่เป็นข้อบังคับสีแดงในเอกสาร
- `next.config.ts` — `API_URL` ถูกอบลง build ต้องมีตั้งแต่ตอน build ไม่ใช่แค่ตอนรัน
- `common/http.ts` — คุกกี้เป็น HMAC ที่ใช้ APP_PASSWORD เป็นคีย์ → สอง service ต้องใช้ค่าเดียวกัน
- `cronAuthorized` fail closed: ไม่ตั้ง CRON_SECRET = ปฏิเสธทุกคำขอ (ปลอดภัย ไม่ใช่บั๊ก)
- `main.ts:30` — api ฟัง `$PORT` ดีฟอลต์ 4010

**ตัวเลขในเอกสารเป็นค่าที่วัดจริงทั้งหมด** — Apify $1.4599/คีย์เวิร์ด/รอบ (แยกรายแพลตฟอร์ม) ·
แปล $0.3479 · จัดหมวด $0.0397 · brand scout $0.4575 · media 42MB/294 รูป

**Learned:** เขียนสคริปต์ python แก้ code fence ให้มีภาษา **แล้วมันลบเนื้อหาในทุก block**
(ลูปเก็บ body ไว้ใน `block` แต่ไม่เคยเขียนกลับลง `out`) จับได้เพราะ IDE ส่ง diff กลับมา
→ เขียนไฟล์ใหม่ทั้งใบแทน และ**ตรวจด้วยสคริปต์ว่าทุก block มีเนื้อหา** (18 blocks, 0 empty)
บทเรียน: การแก้ไฟล์ด้วยสคริปต์ต้องตรวจผลลัพธ์เสมอ ไม่ใช่เชื่อว่า exit 0 = สำเร็จ

## 15:05 — merge เข้า main + push ขึ้น GitHub

**Did:**
- `git rm --cached text.text` + ใส่ `.gitignore` — โน้ตส่วนตัว 45 บรรทัดที่ผู้ใช้ commit เองสองครั้ง
  (`942fe49`, `7e23de8`) ไฟล์ยังอยู่ในเครื่อง แค่ไม่ติดไปกับ repo
- typecheck + **243 เทสต์ผ่าน** ก่อน merge
- `git merge --ff-only` เข้า main (ไม่มี divergence) แล้ว `git push origin main` → `928f79c..ce15ec6`

**ตรวจหลัง push:**
- skill ทั้งสามขึ้น GitHub ครบ (`brand-candidates`, `categorize-listings`, `translate-listing-titles`)
  — จำเป็นเพราะ `claude-plugin/` อยู่นอก `dist/` ถ้าไม่ขึ้น git จะไม่ถึง server
- **ไม่มี `.env`, `text.text`, `.pglite` ใน repo** ยืนยันด้วย `git ls-tree -r origin/main`
- `main` local == `origin/main`

**พบ:** `origin/feat/buying-terms-and-detail-page` อยู่ที่ `942fe49` — **ผู้ใช้ push สาขานี้เองตอน 14:26**
ตอนนี้ main นำหน้า 4 commit และยืนยันด้วย `merge-base --is-ancestor` แล้วว่าไม่มีอะไรเกินจาก main
เป็นสาขาค้างที่ลบได้ (ยังไม่ลบ — เป็นการกระทำที่เห็นจากภายนอก รอผู้ใช้สั่ง)

**State:** main = `ce15ec6` ทั้ง local และ GitHub · working tree สะอาด

## 15:20 — ยกระดับภาษาในระบบเป็นทางการ

**ผู้ใช้:** "อีกเรื่องอย่าเปลี่ยนคำที่ใช้ในระบบเป็นทางการกว่านี้หน่อย"
อ่านว่า "**อยาก**เปลี่ยน...ให้เป็นทางการกว่านี้" — ถามยืนยันก่อนลงมือเพราะถ้าอ่านผิดคือแก้เปล่า
ผู้ใช้ยืนยันว่าถูก **และขอให้ทางการมากกว่าที่ผมเสนอ** (ระดับเอกสารราชการ/รายงานผู้บริหาร)
พร้อมสั่งให้ **คงคำทับศัพท์** (คีย์เวิร์ด แพลตฟอร์ม actor สโมกเทสต์) ไว้เหมือนเดิม

**Did:** แก้ 40 คีย์จาก 577 สตริง ครบทั้ง th/en/zh

| เดิม | ใหม่ |
| --- | --- |
| พอได้ | เหมาะสมปานกลาง |
| น่าสนใจมาก | เหมาะสมสูง |
| ข้อมูลไม่พอ | ข้อมูลไม่เพียงพอต่อการประเมิน |
| ให้ AI วิเคราะห์ | ดำเนินการวิเคราะห์ |
| กดเองเท่านั้น | ดำเนินการด้วยตนเองเท่านั้น |
| ยังไม่มีประวัติพอ | ข้อมูลย้อนหลังไม่เพียงพอ |
| ไม่ทราบช่วง | ไม่ระบุช่วงเวลา |
| น่าสงสัย | ต้องตรวจสอบ |
| หายจากลิสต์ | ไม่พบในผลการค้นหาแล้ว |
| เรตที่ตั้งเอง | อัตราที่กำหนดด้วยตนเอง |
| ข้อมูลนี้บอกอะไรไม่ได้บ้าง | ข้อจำกัดของข้อมูล |
| กด (ทุกที่) | เลือก / ดำเนินการ |

**ผู้ใช้ชี้เฉพาะเจาะจง:** "สินค้าน่าทำแบรนด์ เป็นทางการกว่านี้"
รอบแรกผมเปลี่ยนเป็น "ผลิตภัณฑ์สำหรับแบรนด์ตนเอง" ซึ่งยัง**ไม่ตรงศัพท์ธุรกิจ**
→ ใช้แนวคิด **private label / OEM** แทน:
- `nav.brand` → **ผลิตภัณฑ์แบรนด์ของตนเอง** (en: Private-label candidates)
- `brand.title` → **การคัดเลือกผลิตภัณฑ์เพื่อผลิตภายใต้แบรนด์ของตนเอง**
- `run.kind.brand` → **การวิเคราะห์ผลิตภัณฑ์แบรนด์ของตนเอง**

**แก้ SKILL.md ของ brand-candidates ด้วย** — ถ้าไม่แก้ AI จะยังเขียนโทนเดิมลงหน้าที่เพิ่งทำให้ทางการ
เพิ่มกฎ: เขียนระดับรายงานธุรกิจ ไม่ใช่ภาษาพูด (ดำเนินการ ไม่ใช่ กด · ไม่เพียงพอ ไม่ใช่ ไม่พอ ·
ผลิตภัณฑ์ ไม่ใช่ ของ) แต่ห้ามให้ความเป็นทางการกลายเป็นน้ำ

**ไม่แตะโดยเจตนา:** `ยังไม่มี...` (เป็นภาษาธุรกิจปกติ ไม่ใช่ภาษาพูด) · คำทับศัพท์มาตรฐาน

**Verified:** เมนูข้าง 240px ทุกรายการสูง 37px เท่ากัน **ไม่มีตัดบรรทัด** · 9 หน้าเปิดได้ครบและ
**สแกนแล้วไม่เหลือคำเดิมสักคำ** · มือถือ 375px หัวข้อ 2 บรรทัด ปุ่ม 44px ไม่มี horizontal scroll
**243 เทสต์ผ่าน** (dictionary.test.ts บังคับครบ 3 ภาษาอยู่แล้ว)

## 15:25 — ปิดเซสชัน

**สถานะสุดท้าย:** `main` = `30f7243` · local == GitHub · working tree สะอาด · **243 เทสต์ผ่าน**

**ยังรันค้างอยู่บนเครื่อง (ตั้งใจ ไม่ได้ปิด):**
- Docker `omnix-marketing-db` (Postgres 18) — healthy
- `pnpm dev` (api 4010 · web 3020) — background task `bj43xpahw`

หยุดเมื่อไม่ใช้: `docker compose down` และหยุด dev task

**สรุปค่าใช้จ่ายเครดิต Claude ที่ใช้ไปทั้งเซสชัน ≈ $1.56**
แปล $0.2124 + $0.3479 · จัดหมวด $0.0397 · brand scout $0.0956 + $0.0900 + $0.4575 ·
ทดสอบ skill isolation $0.0740 + $0.0370

**บทเรียนที่ควรจำข้ามเซสชัน:**
1. **วัดก่อนออกแบบเสมอ** — ทุกครั้งที่เดาจาก fixture แล้วไม่ตรวจข้อมูลจริง ผมพลาด
   (บันไดราคา "น่าจะชั้นเดียว" → จริง 3 ชั้น 15 แถว · MOQ "ส่วนใหญ่น่าจะสูง" → จริงส่วนใหญ่เป็น 1)
2. **อย่าเชื่อคอมเมนต์ตัวเอง** — เขียนว่า `--plugin-dir` แยก skill ของโฮสต์ออก ทดสอบจริงแล้วไม่จริง
3. **เทสต์ที่ไม่เคยแดงไม่ได้พิสูจน์อะไร** — `source-hygiene` ผ่านทั้งที่มีอักขระต้องห้ามอยู่ในไฟล์
   เพราะช่วงที่เขียนไว้ไม่ครอบคลุม ต้องใส่ probe ให้มันแดงก่อนถึงจะเชื่อได้
4. **สแกนทั้งไฟล์ อย่าเดาบรรทัด** — ตรวจ code review ข้อ 13 ผิดเพราะ grep เฉพาะบรรทัดที่มี "0300"
5. **แก้ไฟล์ด้วยสคริปต์ต้องตรวจผลลัพธ์** — สคริปต์เติมภาษาให้ code fence ลบเนื้อหาทั้งหมดแต่ exit 0

## 15:50 — ขยายจำนวนรายการที่เสนอ 6 -> 12

**ผู้ใช้ถามก่อนว่าทำไมมี 6** — ตอบด้วยตัวเลขจริง: มีการกรอง 3 ชั้น
135 สินค้า → ตัด 26 ที่ไม่มียอดขาย/ราคา → เหลือ 109 → `perBucket = floor(60/4) = 15` ต่อ
(แพลตฟอร์ม+ช่วงเวลา) → **ส่ง AI แค่ 50** (1688 15 · douyin 15 · xhs 15 · temu 5)
→ skill สั่ง "ไม่เกิน 6" → ได้ 6
แปลว่า **59 รายการที่ใช้วิเคราะห์ได้ไม่เคยไปถึง AI เลย**

**Did:**
- `SKILL.md` 6 → **12** และที่สำคัญกว่าคือลบประโยคที่ดันให้เสนอน้อย
  เดิม: "the fewest genuinely worth the merchant's time" → **ต่อให้ขยับเลขก็ยังเสนอ 6**
  ใหม่: เสนอทุกรายการที่พ่อค้าจะเปิดดูจริง · ห้ามเติมให้ครบ · ห้ามหักออกเพื่อให้ลิสต์สั้น ·
  รายการที่อ่อนกว่าให้อยู่ใน picks พร้อม `confidence: low` ดีกว่าตัดทิ้งเงียบ ๆ
- `brand.ts` `slice(0, 8)` → `MAX_PICKS = 12` (export)
- **เทสต์ใหม่กันเพดานสองที่หลุดจากกัน** — อ่านตัวเลขจาก SKILL.md มาเทียบ `MAX_PICKS`
  พิสูจน์แล้วว่าแดงจริงด้วยการตั้งเป็น 9 ชั่วคราว

**ผลรันจริง:** picks **6 → 12** · avoid 1 · ระดับ medium 6 / low 6
**ค่าใช้จ่าย $0.6541** (เดิม $0.4575 — เพิ่ม 43% เพราะ output ยาวขึ้น ไม่ใช่ input)

**ข้อสังเกต:** 6 รายการที่เพิ่มมาเป็น `low` ทั้งหมด ซึ่งถูกต้องตามกฎ — เกือบทั้งหมดเป็น 1688/xhs
ที่ยอดขายไม่ระบุช่วงเวลาและไม่มีเทรนด์ โมเดลไม่ได้ยกระดับความมั่นใจเพื่อเติมลิสต์

**Verified:** 1440px — 12 การ์ด รูปขึ้นครบ 12 สูงใบละ 184-206px หน้ายาว 3,166px ไม่มี horizontal scroll
**245 เทสต์ผ่าน** (api 137 · web 108)

## 16:10 — ปรับ UI หน้า /trends

**อาการที่ผู้ใช้ส่งภาพมา:** ทุกแท่งมีป้ายว่า "สายนาฬิกา..." เหมือนกันหมด แยกไม่ออกว่าตัวไหนคืออะไร

**ต้นเหตุ:** `HBarChart` มี `tickFormatter` ตัดชื่อที่ **9 ตัวอักษร** (`v.slice(0, 9)`)
และ **"สายนาฬิกา" ยาว 9 พอดี** → ทุกชื่อกลายเป็นคำเดียวกัน
ซ้ำร้าย `slice` ตัดตาม code unit ซึ่งแยกพยัญชนะออกจากสระ/วรรณยุกต์ไทยได้ → glyph เพี้ยน

**Did:**
- `ellipsize()` ตัดตาม **grapheme cluster** ด้วย `Intl.Segmenter` แทน `slice`
- `labelWidth` / `maxLabelChars` เป็น prop — **วัดจากข้อมูลจริงแล้วว่าต้อง 34 ตัวอักษร**
  ถึงจะไม่ซ้ำ (ที่ 30 ยังซ้ำ 1 คู่ เพราะสองชื่อแรกต่างกันที่ตัวอักษรที่ 31)
- **เพิ่มตัวเลขบนแท่ง** (`LabelList`) พร้อมเครื่องหมาย — เดิมอ่านไม่ออกเลยว่า 20% หรือ 800%
- **ขาลงใช้สีเทา** (`colorIndex: 5` → `chart-other`) ให้ตรงกับแท่งในตารางที่ทำไว้อยู่แล้ว
  และ**เครื่องหมายลบอยู่ในตัวเลข** ไม่ได้สื่อด้วยสีอย่างเดียว (กฎ OMNIX)
- หัวข้อ `trends.top10` → `trends.topN` — เดิมเขียน "10 อันดับแรก" ทั้งที่มี 8 กับ 4
- แถวสูงจาก 32 → 40px ให้ตัวเลขไม่ชนกัน

**ปัญหาที่เจอระหว่างทาง 2 ข้อ:**
1. **ส่งฟังก์ชัน `valueLabel` ข้าม server → client ไม่ได้** (กฎเดียวกับที่พลาดกับ `ProductGallery`
   เมื่อเช้า) → เปลี่ยนเป็น `valueKey` ชี้ไปที่สตริงที่จัดรูปแบบไว้แล้วในข้อมูล
2. **ResizeObserver ไม่ยิงเลย** ในแท็บทดสอบ — ทดสอบด้วย observer ที่สร้างสดในหน้าแล้วก็ไม่ยิง
   (แท็บไม่ได้ถูก paint) แต่การพึ่ง observer ให้ paint แรกถูกต้องก็เปราะอยู่ดี
   → เปลี่ยนเป็น `matchMedia("(min-width: 768px)")` ซึ่งได้คำตอบก่อน paint
   **เริ่มที่ false เสมอ** เพราะแกนกว้างเกินบนมือถือทำให้แท่งหายทั้งหมด ส่วนแกนแคบชั่วครู่บนเดสก์ท็อปแค่ดูโล่ง

**ตัดสินใจ: ซ่อนกราฟบนจอ < 768px** (`.ap-wide-only`)
วัดแล้วที่ 375px แกน 132 + ขอบขวา 72 เหลือแท่งแค่ ~105px จาก 309 และชื่อเหลือ 15 ตัวอักษรจนซ้ำกัน 2 คู่
ตารางข้างล่างมีข้อมูลมากกว่าอยู่แล้ว (ชื่อเต็ม · แพลตฟอร์ม · ยอดขาย · เดลต้า · แท่งเปรียบเทียบ)

**Verified:** 1440px — ขาขึ้น 8/8 ชื่อไม่ซ้ำ สีม่วง · ขาลง 4/4 สีเทา · ตัวเลขครบทุกแท่ง ·
375px — กราฟซ่อน ตาราง 12 แถวพร้อมแท่ง ชื่อเต็ม ไม่มี horizontal scroll
**245 เทสต์ผ่าน**

## 16:20 — เปลี่ยนชื่อฟีเจอร์เป็น "ผลิตภัณฑ์น่าสนใจ"

**ผู้ใช้สั่ง:** "ผลิตภัณฑ์แบรนด์ของตนเอง เปลี่ยนเป็น ผลิตภัณฑ์น่าสนใจ"

แก้ทุกที่ที่ใช้วลีนั้น ครบ 3 ภาษา:
- `nav.brand` → ผลิตภัณฑ์น่าสนใจ / Products of interest / 值得关注的产品
- `nav.short.brand` → น่าสนใจ / Of interest / 值得关注
- `run.kind.brand` → การวิเคราะห์ผลิตภัณฑ์น่าสนใจ
- `errors.brand.noSkill` → ตามไปด้วย

**คงไว้โดยตั้งใจ:** `brand.title` = "การคัดเลือกผลิตภัณฑ์เพื่อผลิตภายใต้แบรนด์ของตนเอง"
เมนูเป็นชื่อเรียกสั้น หัวข้อหน้าเป็นตัวอธิบายว่า "น่าสนใจ" หมายถึงน่าสนใจ**ในแง่ไหน**
ถ้าตัดทิ้ง คำว่า "น่าสนใจ" จะลอยไม่มีบริบท — แจ้งผู้ใช้ไว้แล้วว่าเปลี่ยนได้ถ้าต้องการ

**Verified:** เมนูสูง 37px ไม่ตัดบรรทัด · ไม่มี horizontal scroll · หน้ายังแสดง 12 การ์ด
**245 เทสต์ผ่าน**
